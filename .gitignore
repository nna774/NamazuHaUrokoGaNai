# PlatformIO
firmware/.pio/
firmware/.pioenvs/
firmware/.piolibdeps/
firmware/.vscode/
firmware/**/secrets_provision.h
docs/piezo_phase0/.pio/
docs/piezo_phase0/out.log

# デバイスマニフェスト（HMAC鍵を含む。雛形は devices.example.json）
tools/devices.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfvars
!terraform/*.tfvars.example
terraform/.terraform.lock.hcl
terraform/builds/

# Lambda build artifacts
lambda/**/build/
lambda/**/*.zip

# 検出限界マップArtifact用の生成物（tools/detection_map_svg.pyで再生成可能・貼り込み後は不要）
docs/log/img/detection-map-*

# Node / dashboard
dashboard/node_modules/
dashboard/dist/
dashboard/config.js

# Captured data / scratch
*.csv
!tools/**/sample_*.csv
!firmware/partitions_*.csv
!tools/detection_events.csv
data/
*.bin
!firmware/**/*.bin.example

# S3生波形のローカルキャッシュ（解析セッション間で使い回す。git管理対象外）
.s3cache/

# Claude Code（作業用 worktree の置き場。設定を共有したくなったら .claude/ 自体は追跡する）
.claude/worktrees/

# OS
.DS_Store

# direnv（ローカルのAWS_PROFILE切り替え等、マシン依存の設定）
.envrc
