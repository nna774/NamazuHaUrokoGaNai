# 進捗

新しいものが上。各行の詳細は `log/` の該当ファイルにある。
**このファイルは索引だ。判断の理由は各ログに、現在の結論は各設計ドキュメントにある。**

| 日付 | 何が決まったか | 詳細 |
|---|---|---|
| 2026-08-06 | **pull型OTAをdevice2実機で通しで試し、TLS証明書検証（2段階失敗の末Amazon Root CA 1埋め込みで解決）・失敗時の高頻度リトライ（測定停止を招いた。1分バックオフで解消）・停滞の未検知（watchdogにSlack通知を追加）の3件を直した。** 実際に取得〜書き込み〜再起動〜新バージョンでの起動まで成功し、pull型OTAが実機で動いた最初の記録になった | [log/2026-08-06-ota-pull-device2-live-test.md](log/2026-08-06-ota-pull-device2-live-test.md) |
| 2026-08-06 | **pull型OTAのトリガーを作戦どおり（バッチ送信レスポンスへの便乗）に戻した。** 直前の実装で「api Lambdaへの独立GET」に無断で設計変更していたことをユーザーに指摘され、batch-uplinkを拡張（`Uploader::watchResponseHeaders`で複数ヘッダ監視に対応、[PR#5](https://github.com/nna774/batch-uplink/pull/5)・[v1.5.0](https://github.com/nna774/batch-uplink/releases/tag/v1.5.0)）して当初案どおりに作り直した。`firmware/platformio.ini`・`terraform/build_lambda.sh`のpinをv1.5.0へ揃えた。教訓: 設計を実装都合で無断変更しない、必ず確認する | [log/2026-08-06-ota-pull-trigger-revert-to-header.md](log/2026-08-06-ota-pull-trigger-revert-to-header.md) |
| 2026-08-06 | **HTTPSプル型OTAを実装した。** 前提のNVS化（デバイス識別情報・秘密・エンドポイントURLをコンパイル時定数からPreferencesへ移行、`[env:provision]`/`[env:adxl355-provision]`で書き込み）・ビルド時バージョン埋め込み（gitの短縮hash、`get_fw_version.py`）・`tools/request_ota.py`（手元から更新許可）・api Lambdaでの`pending_ota_version`公開・ファームのOTAチェック(5分ごとのapi問い合わせ)+`esp_https_ota`取得（TLSはArduino-ESP32同梱のルートCAバンドルで検証）・`tools/publish_ota.sh`（既存ダッシュボードCloudFront/S3への配布）を実装。トリガーは当初案（バッチ送信レスポンスへの便乗）から、batch-uplinkを変更せずに済むapi Lambdaへの独立GETに変更した。ロールバックは実機無しでは安全に検証できず見送り。firmwareビルド全env・`test/run.sh`・pytest(113件)は確認済み。**実機での動作確認はまだ** | [log/2026-08-06-ota-pull-implementation.md](log/2026-08-06-ota-pull-implementation.md) |
| 2026-08-06 | **pull型OTA作戦を訂正: バイナリに秘密情報が生で入っていると判明した。** `firmware/src/secrets.h`はWiFi SSID/パスワード・投稿用HMAC鍵・OTAパスワードをコンパイル時の文字列定数として持ち、焼いたbinに平文で入る。push型は送り返す相手が秘密の持ち主自身なので問題にならないが、pull型で「envごとに1本を公開URLに置く」設計は特定デバイスの秘密を世界に漏らすことになり成立しない。対策として秘密情報をNVS化（初回USB書き込み時に別途書く、OTAはappパーティションのみ書き換えるので保持される）する方針を、pull型実装の前提条件として追加した | [log/2026-08-06-ota-pull-secrets-in-binary.md](log/2026-08-06-ota-pull-secrets-in-binary.md) |
| 2026-08-06 | **HTTPSプル型OTA（外出先からの更新・無人運用向け）の作戦をまとめた（実装は未着手）。** トリガーは自律ポーリングではなく手元からの明示許可方式を採用（配布物の書き込み権限侵害時のブラスト半径を抑えるため）。伝達経路はリモート再起動と同型だが値は消費せず照合し続ける設計にし、それ自体がリトライ機構になる。配布物は既存CloudFront+S3に相乗り。ロールバックはpull型で初めて`CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE`の価値が出ると判断した | [log/2026-08-06-ota-pull-strategy-design.md](log/2026-08-06-ota-pull-strategy-design.md) |
| 2026-08-06 | **device 2にOTA対応ファームをUSB書き込みし、push OTAが自宅ネットワークのVLAN分離で母艦から届かないと判明した。** USB書き込み・起動・ArduinoOTAリスナー起動（`namazu-2.local`）・クラウド受信再開は実機で確認できたが、母艦(`10.8.30.0/24`)からdevice 2(`10.255.255.0/24`, `unnamed_network_g`)へのespota転送はICMPは通るのにUDP往復が無応答。ゲスト系VLANのクライアント分離が疑わしい。ファーム実装の不具合ではなく運用上の制約として`docs/ota.md`に記録した | [log/2026-08-06-ota-network-isolation.md](log/2026-08-06-ota-network-isolation.md) |
| 2026-08-06 | **OTA更新（ArduinoOTA、LAN内push）を実装した。** batch-uplinkに`Uploader::flushToSpill()`を追加してv1.4.0を切り（[PR#4](https://github.com/nna774/batch-uplink/pull/4)）、firmwareに焼く前のRAMキュー退避＋測定停止シーケンスを実装。`tools/provision_device.py`にデバイス別`ota_password`を追加し、`platformio.ini`に`esp32dev-ota`/`adxl355-ota`envを足した。同じ`flushToSpill()`をリモート再起動にも配線し直し、通信完了を待たず数秒で再起動できるようにした。firmwareビルド(esp32dev/adxl355/両-ota/両-sensortest)・`test/run.sh`・`pytest`(113件)は確認済み。**実機での動作確認はまだ** | [log/2026-08-06-ota-arduino-ota.md](log/2026-08-06-ota-arduino-ota.md) |
| 2026-08-06 | **リモート再起動を実装した。** batch-uplinkに`devices.request_restart`/`Uploader.watchResponseHeader`を追加してv1.3.0を切り、ingest/tools(`request_restart.py`)/firmware(`main.cpp`)を実装した。firmwareビルド(esp32dev/adxl355両env)・`pytest`(111件)は確認済み。**実機での動作確認はまだ** | [log/2026-08-06-remote-restart-implementation.md](log/2026-08-06-remote-restart-implementation.md) |
| 2026-08-06 | **リモート再起動（コマンドラインから要求→デバイスがバッチ送信時に検知して自分で再起動）の設計をまとめた（実装は未着手）。** 伝達経路はバッチ送信レスポンスへの便乗を採用。batch-uplinkの`Uploader::postBatch`にレスポンスヘッダ読み取りを追加する必要があるが、`dropOldestWhenFull`と同じくオプトイン方式にしてElectabuzz側の挙動は変えない設計にした | [log/2026-08-06-remote-restart-design.md](log/2026-08-06-remote-restart-design.md) |
| 2026-08-05 | **TFT画面右下のbacklog表示を「件数+経過時間」(`buf:12 18m`)に拡張した。** 件数は`spillCount()`だけでなく`spillCount()+ramQueued()`の合計に修正（従来RAM分が抜けていた）。経過時間はbatch-uplinkに追加した`Uploader::oldestQueuedStartUs()`([batch-uplink#2](https://github.com/nna774/batch-uplink/pull/2)、マージ済み・`v1.2.0`)を使う。`lib_deps`/`UPLINK_VERSION`をv1.2.0に上げてビルド確認済み。**実機での目視確認はまだ** | [log/2026-08-05-display-backlog-age.md](log/2026-08-05-display-backlog-age.md) |
| 2026-08-05 | **batch-uplinkに`v1.1.0`タグを切ってNamazu側(`lib_deps`/`UPLINK_VERSION`)を追従させ、`main.cpp`に`dropOldestWhenFull=true`を配線した。** その過程で、直前にマージしたPR#4に本来含まれるはずのflash_size修正コミットが1本漏れていた（マージ後にpushしたため）と判明し、cherry-pickで直接masterに修正を入れた。マージ操作は「ボタンを押した時点のHEAD」しか取り込まないという教訓 | [log/2026-08-05-uplink-v1.1.0-and-merge-gap.md](log/2026-08-05-uplink-v1.1.0-and-merge-gap.md) |
| 2026-08-05 | **device2のspill(LittleFS退避)実容量が既定4MB前提のコメント(`config.h`の「90日ぶんの上限目安」)と合っていないと判明。esptool flash_idで実機を物理確認したところ実は16MBあり、`firmware/partitions_adxl355_16mb.csv`（OTA用app0/app1各2MB・spiffs約11.88MB）に書き換えて実機で正常動作（起動・WiFi接続・送信再開）まで確認した。** spill実容量は約19.5分→**約168.8分（約2時間49分）**。**PlatformIOのflash_size上書きキーは`board_build.flash_size`ではなく`board_upload.flash_size`**（前者は黙って無視される。これで一度起動ループを踏んでからの復旧＋原因特定を経ている）。それでも溢れる場合に備え、**batch-uplinkに「spill満杯なら古いデータから捨てる」動作をオプトインで追加する方針**（Electabuzzの既定動作は変えない） | [log/2026-08-05-device2-spill-overflow.md](log/2026-08-05-device2-spill-overflow.md) |
