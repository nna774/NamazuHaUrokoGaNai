# 進捗

新しいものが上。各行の詳細は `log/` の該当ファイルにある。
**このファイルは索引だ。判断の理由は各ログに、現在の結論は各設計ドキュメントにある。**

| 日付 | 何が決まったか | 詳細 |
|---|---|---|
| 2026-08-20 | **複数デバイスのイベントを「同じ地震」として手動で相互リンクする機能を実装した。** 「地震が起きたら全デバイスの波形をイベント保存したい」から検討を始め、detect確定時に他機のrawも自動コピーする案（confirm時の即時コピー＋定期リトライの2段構成）まで設計を詰めたが、当面は`promote_event.py`での手動イベント化が主になりそうという運用判断から自動化は保留。`tools/flag_event.py relate`/`unrelate`で`related_events`フィールド（対称・event_idリスト）を張り、api・ダッシュボード（イベント詳細に「関連イベント」リンク）まで通した | [log/2026-08-20-relate-events-across-devices.md](log/2026-08-20-relate-events-across-devices.md) |
| 2026-08-19 | **補強検知の代替センサ候補として、質量-バネ系+連続出力ピックアップの3案（光てこ・磁気ピックアップ(ホール素子)・オプティカルフローセンサ)を雑談ベースで検討した（未着手）。** GY-530等のToF距離センサは「建物内の2点間相対距離」しか測れず地震動では剛体的に一緒に動いて差分が出にくいと判断し不採用、SW-58010P(振動スイッチ)も感度調整不可で既存の「傾斜/ボール/水銀スイッチ」枠と同評価に帰着すると判断。3案はいずれもgeophoneと同じ質量-バネ系(f0チューニング・制動比設計)が前提で違いはピックアップ方式のみ、オプティカルフローは錘側に取り付けて初めて意味を持つ(フレーム固定で床を見る使い方は不可)という注意点を明記し、磁気ピックアップ用ホールIC・オプティカルフローセンサの具体的な部品候補も見繕った | [log/2026-08-19-other-sensors-more-ideas.md](log/2026-08-19-other-sensors-more-ideas.md) |
| 2026-08-17 | **`batch_uplink.s3util`を`lambda/common/s3util.py`へ引き上げ、`UPLINK_VERSION`を`v3.0.0`に上げた。** 名目上はElectabuzzと共有していたが、実際はElectabuzzが保存方針の違いから独自の`s3keys.py`を最初から持っており使われていなかった(逆に`notify`はElectabuzzのwatchdog Lambdaが2026-08-16に実装され実際に使い始めている)。batch-uplink側の削除PR([#24](https://github.com/nna774/batch-uplink/pull/24))はマージ・`v3.0.0`タグ付け済み | [log/2026-08-17-pull-in-s3util.md](log/2026-08-17-pull-in-s3util.md) |
| 2026-08-15 | **Z軸の回路+機械共振+ADCサンプリングを通したシミュレーション(`tools/piezo_lowpass_sim.py`)を組み、本線と同じ1kHzオーバーサンプリング+ボックスカー10平均を追加する案を検討した(将来検討・未実装)。** 地震動シミュレーションでは「初動レベルの揺れなら静穏帯からは浮き上がりそう」という一次近似の目安が得られた(未校正のため目安に留まる)。オーバーサンプリング+ボックスカーの追加は、P波初動直後の「共振がナイキスト直下で叩き起こされサンプル毎に0⇄1000へ飛ぶ」エイリアシングの暴れを大きく抑える計算になり(ピーク約1/3)、2026-08-12の既存結論(連続トーンには51〜64%しか効かない)と矛盾しない——今回の効き目はインパルス性の立ち上がりのエイリアシング自体を1kHz標本化で潰す効果と判断。ただしESP32-C3のシングルコア+WiFi環境で1kHzサンプリングが安定するかは実機確認が必要 | [log/2026-08-15-piezo-oversampling-boxcar-consideration.md](log/2026-08-15-piezo-oversampling-boxcar-consideration.md) |
| 2026-08-15 | **Z軸ローパスのC値をC=200nF→100nFへ半減し、経過観察を始めた。** 「200nFはクランプ回避に必要な最小限より過剰では」という疑問を、その夜の風がクランプ発生時と近い状況だったのを機に実地で検証。最初の測定(0:15スタート)ではクランプ再発・大きなスパイクが出たが、開始時刻を1分ずらすとクランプ0件に一変——コンデンサを抜く操作自体の機械的・静電的な衝撃が窓の先頭に混入したアーティファクトと判明した。クリーンなデータではC=100nFでもクランプ無く、C=200nFより最大ppもむしろ小さかった。一晩・一風況の結果に過ぎず結論はまだ出ていないが、C=100nFのまま経過観察する方針とした | [log/2026-08-15-piezo-lowpass-value-tuning-experiment.md](log/2026-08-15-piezo-lowpass-value-tuning-experiment.md) |
| 2026-08-14 | **【予想・未検証】ピエゾローパスの追加コンデンサが、周波数選択フィルタではなく容量分圧器として効いている可能性に気付いた。** 当初の`fc`計算はセンサ側(Node A)を理想電圧源とみなす近似だったが、ピエゾ自身も静電容量を持つ弱い電圧源であり、C2がその容量より大きいと周波数に関係なく効く容量分圧(≈1/(1+C1/C2))が主効果になりうる。Z軸(C1未実測の仮定20nF)はk=10で≈-20.8dB、LDT0-028K(§9冒頭、C1=480pFはデータシート既知)はk=56.25で≈-35dBの一律減衰になる計算——後者は未実装なので設計を見直す余地がある。対応案(C2をC1近くまで縮めて減衰器と割り切る/巨大抵抗で真に選択的にする/バッファ追加)を検討、方針は未決定 | [log/2026-08-14-piezo-lowpass-capacitive-divider-hypothesis.md](log/2026-08-14-piezo-lowpass-capacitive-divider-hypothesis.md) |
| 2026-08-14 | **docs横断で陳腐化した記述を6箇所直した。** `STATUS.md`の残タスク(完了済みADXL355 2号機項目の削除)・日付ヘッダ更新、`other-sensors.md`(piezo.mdへの引き継ぎ明記)、`device_overlay.md`(冒頭「進捗」欄と「着手順」節が実装済みのbと矛盾していたのを是正)、`design.md`/`wire_format.md`(sensor_type帯域`128〜249`が「未実装」のままだったのを実装済みに更新)、`CLAUDE.md`のドキュメント一覧表の該当2行を更新 | [log/2026-08-14-docs-staleness-sweep.md](log/2026-08-14-docs-staleness-sweep.md) |
| 2026-08-14 | **既存Z軸(圧電ブザー素子、Rs=47kΩ)側のアンチエイリアシングローパスを設計・実装し、実データでの切り分け実験まで完了した。** LDT0-028K側(§9、Rs=220kΩ)は共振周波数未実測のまま20〜30Hzに絞る手探りの設計だったが、Z軸は共振周波数を既に実測済み(49.947Hz、§5)なので同じ手法(既存Rsを流用しNodeB-GND間へC追加)をより直接的に適用できた。手持ちの詰め合わせが100nFまでしか無かったため100nF×2並列でC=200nF(fc≈16.93Hz)としてブレッドボード実装。実装直後の静穏化がカンチレバー付け直し(同じ作業中に脱落・再固定)の効果ではないかという懸念に対し、ファーム側(ADC読み取りは無変更と確認)を容疑から外した上で、コンデンサだけを一時的に抜き差しするAPI `/recent` ベースの切り分け実験を実施。カンチレバー固定のままコンデンサの有無だけで静穏⇄騒がしいが再現し、ローパスが静穏化の主要因と確認した。理論上の1次RC減衰(-9.4dB)より遥かに大きい実測改善は、フィルタ前のADCクランプ飽和が解消されたことによる非線形な上乗せと推定 | [log/2026-08-14-piezo-buzzer-lowpass-design.md](log/2026-08-14-piezo-buzzer-lowpass-design.md) |
| 2026-08-14 | **`docs/STATUS.md`のデバイス一覧テーブルにdevice_id=3（ピエゾ実験機）が漏れていたのを直した。** 2026-08-11にテーブルを新設した翌日(08-12)にphase1まで完了したdevice 3が反映されないまま残っていた | [log/2026-08-14-status-md-missing-device3.md](log/2026-08-14-status-md-missing-device3.md) |
| 2026-08-13 | **LDT0-028K用ADC手前のアンチエイリアスRCローパスの定数を、実機到着前(共振周波数未実測)に決めた。** 既存保護回路のRs=220kΩを流用しコンデンサ1個(C=27nF候補)を追加するだけの1次RCローパスとし、カットオフは1-10Hz帯を削らない下限とナイキスト(50Hz)より低い上限の2つの既知の制約から20〜30Hzに絞った。1次フィルタでは急峻に落とせずmass=1(90Hz→畳み込みで10Hz直撃)は-10.8dB程度しか削れないため、対策の主軸は引き続き風防(§7の軸1)であることを確認した | [log/2026-08-13-ldt0-028k-lowpass-filter-design.md](log/2026-08-13-ldt0-028k-lowpass-filter-design.md) |
| 2026-08-12 | **ピエゾ実験機の共振ノイズをナイキストの観点で捉え直し、対策方針(風防+アナログローパス)を決めた。** `piezo_config.h`のコメント「ナイキストに余裕を持たせて100Hzにした」が誤りと判明——固有振動(約50Hz)はfs=100Hzのナイキスト(50Hz)とちょうど重なっており余裕は無い（地震動本体(1-10Hz)への影響は無し、実害無し）。新規発注したLDT0-028Kのmass別共振周波数をfs=100Hzで畳み込むと、mass=1(90Hz)がちょうど1-10Hz帯の上端(10Hz)に直撃すると判明し、以前の推奨(mass=1か2)を見直す材料になった。本線と同じオーバーサンプリング+間引き(ボックスカー平均)を転用する案も検討したが、周波数応答を計算すると50〜60Hz帯はほぼ素通し(51〜64%)でLDT0-028Kの狭帯域共振には効果が薄いと分かり、主軸は風防(励起源を減らす)とADC手前のアナログRCローパス(fsに依存せず除去可能にする)に決めた | [log/2026-08-12-piezo-resonance-noise-diagnosis-and-mitigation-plan.md](log/2026-08-12-piezo-resonance-noise-diagnosis-and-mitigation-plan.md) |
| 2026-08-12 | **watchdogのデータ遅延判定・dashboardの「データ鮮度」表示が誤って暴れるバグを直した。** RAM優先送信(v2.11.0)以降、backlog処理中はRAM(最新)→spill(古い)が交互に届き、`last_batch_start_us`が無条件上書きのため過去へ巻き戻っていた。`batch-uplink`側で条件付き更新による単調増加に変更([PR #23](https://github.com/nna774/batch-uplink/pull/23)、`v2.14.0`)、firmware/terraformのpinを追従させ再デプロイした | [log/2026-08-12-lag-monotonic-fix.md](log/2026-08-12-lag-monotonic-fix.md) |
| 2026-08-12 | **device3(ピエゾ)に、本線main.cppが送っている可観測性ヘッダ全部(版数・稼働時間・ヒープ・再起動理由・未送信バックログ)とリモート再起動監視、pull型OTAを移植した。** phase1実装時に「今回見送り」としていたOTAを、`fw_version`が空文字のままだった実運用上の不便を機に対応した。シングルコア(ESP32-C3)でも安全停止シーケンス・TLS二重接続回避とも本線と同じロジックで成立。`get_fw_version.py`の`_ota_env()`が`piezo`を認識しない不具合(直さないと配布URLを取り違える)も修正、`publish_ota.sh`もpiezo対応。パーティション構成を実ビルドで確認し本線と同じOTA対応レイアウト(app0/app1各1.25MB)と判明。`pio run`5env・golden test確認済み、**実機投入・実OTA転送確認はまだ** | [log/2026-08-12-piezo-ota-and-observability-headers.md](log/2026-08-12-piezo-ota-and-observability-headers.md) |
| 2026-08-12 | **dashboardのライブ画面で、非校正センサ(ピエゾ=device3)選択時にgal前提の表示（概算震度・縦軸のgal単位）を出さないようにした。** detect Lambdaと同じ`wire.is_calibrated()`を単一の真実として`/devices` APIに`calibrated`真偽値を追加、dashboardはこれで概算震度計算をスキップし縦軸表示から「gal」を落とす | [log/2026-08-12-dashboard-non-calibrated-display.md](log/2026-08-12-dashboard-non-calibrated-display.md) |
| 2026-08-12 | **ピエゾ実験機(device3)、風による反応をAPI生データで確認した。** ライブ画面で山が繰り返し出ているのを見て「地震か」という当初の見立てに対し`/recent`のJSONを直接取得。device1・2(本物の加速度計)は同時刻ノイズフロアのままで地震ではないと確認。device3はFFTで0.03〜0.1Hz(風の息の周期)のエンベロープと約50Hz(カンチレバー固有振動、`docs/piezo.md`§5実測と一致)のリンギングが重なって見え、机を叩いた時と同じ「衝撃→減衰」シグネチャが繰り返し出ている形と判明。窓・玄関・架台(コンクリートブロック)は目視で揺れていないというユーザーの現場確認から、むき出しのカンチレバー本体が風で直接励振されている可能性が高いと判断。値は12bit ADC生値(0〜4095、gal単位ではない)で、張り付きは保護回路の設計通りの安全な飽和挙動であり破損の兆候ではないことも確認 | [log/2026-08-12-piezo-wind-sensitivity-observation.md](log/2026-08-12-piezo-wind-sensitivity-observation.md) |
| 2026-08-12 | **LDT0-028Kを2個使い、既存Z軸(圧電ブザー)に加えてX/Y水平2軸を足す3軸化を検討し、ピンアサインを予約した。** X=GPIO0・Y=GPIO1(ADC1の空きピン、GPIO3は予備)を予約、固定はデータシート推奨のcrimped contacts直付けユニバーサル基板方式(ユーザー提案)に合意、device_idは新規に割らず既存device_id=3を再利用する方針、RAMは1軸6KB→3軸で概算18KB程度でkMaxRamBatches=1のまま収まる見込みと見積もった。配線・ファーム実装・実機確認はまだ | [log/2026-08-12-piezo-phase1-impl.md](log/2026-08-12-piezo-phase1-impl.md) |
| 2026-08-12 | **LDT0-028K導入時の保護回路図（Rs=220kΩ, Rb=430〜500MΩ, D1/D2=1N60）を作成した。** ピエゾブザー機の保護回路図(§4)と同じトポロジーで、値をデータシート実測ベースの見積もりに差し替えた。Rbは430MΩ・500MΩどちらでも技術的に問題ないため未確定のまま両方併記、実際の抵抗値・購入判断はユーザー裁量 | [log/2026-08-12-piezo-phase1-impl.md](log/2026-08-12-piezo-phase1-impl.md) |
| 2026-08-12 | **ピエゾ実験機、phase1（クラウド統合）を実装し実機確認まで完了した。** Lambda側3箇所(wire.py/detect/api)は設計通りに実装、pytest 135件通過。firmware側は実装中にESP32-C3スーパーミニがシングルコアと判明し、本線main.cppのデュアルコア分離前提が使えないため、別エントリポイント`piezo_main.cpp`に測定/送信タスクをキュー経由の2タスク構成で新規実装する形へ方針を修正した。スピル・自動リトライはUploaderの標準機能で追加実装不要、AccelSensorは3軸gal前提のため継承せず新たにRawSensor抽象を新設。device_id=3を払い出しterraform apply、実機書き込み後、起動時空きヒープ162KB(懸念していたRAM不足の兆候なし)、クラウドへの到達(`/devices/3`でonline)、dashboardでの波形表示(y,z列0埋めpaddingも実データで確認)まで確認できた | [log/2026-08-12-piezo-phase1-impl.md](log/2026-08-12-piezo-phase1-impl.md) |
| 2026-08-12 | **ピエゾ実験機、phase1（クラウド統合）の設計方針を決めた。** `other-sensors.md`で未定だった物理量前提ロジックの迂回判定は、detect Lambdaがバッチの`sensor_type`を既に手元に持っているためDynamoDB参照不要と判明。`SENSOR_TYPE_PIEZO=128`（加速度センサ帯0-127と分離）、ワイヤ形式は`axes:1`で正直に送りdashboard対応はapi Lambdaの`_waveform_payload()`1箇所のpaddingに閉じる、`device_prompt`自動生成はファームに実装しなければ安全、firmwareは本線へ統合、という方針にした | [log/2026-08-12-piezo-phase1-plan.md](log/2026-08-12-piezo-phase1-plan.md) |
| 2026-08-11 | **ピエゾ実験機（着手からphase0達成・機械設計検証まで）。** 部品箱のピエゾ+ESP32-C3スーパーミニで実機を組み、実測に基づく保護回路(Rs=47kΩ・Rb=10MΩ・D1/D2=1N60)を設計。GPIO4未接続の誤測定を一度踏んで訂正した後、実接続でphase0(反応確認)を達成。カンチレバー(割り箸)+おもり(金属リング)を実装し間接振動への反応と固有周波数(約50Hz、1-10Hz帯から十分離れている)も確認し、センサ+保護回路+マイコン+機械設計の一通りの検証が完了した | [log/2026-08-11-piezo.md](log/2026-08-11-piezo.md) |
| 2026-08-11 | **校正不要・タイミング一致の裏取りだけを狙う安価な代替センサ（圧電・歪みゲージ等）の構想を雑談ベースで検討した（未着手）。** 候補比較の結果、圧電素子(ピエゾ)が部品代・回路・工作の全てで最有力と判断。設計面では「配送層(batch-uplinkの認証・時刻同期・S3格納)は校正の有無に関係なく使い回すのが合理的で、危ないのは物理量前提のロジック(計測震度計算・ダッシュボードのライブ概算・イベント自動生成のgalしきい値)の方だけなので、そこだけdevice_id単位で迂回させればよい」という一般化できる方針に落ち着いた | [log/2026-08-11-other-sensors-exploration.md](log/2026-08-11-other-sensors-exploration.md) |
| 2026-08-11 | **device1のコンクリブロック固定(08-09)前後で背景ノイズ・生活振動スパイクをS3生波形から比較した。** 固定前(複数日)と固定後(まだ2日ぶん)を比較し、broadband RMS(特に水平y軸)は夜昼とも固定前の日々のばらつきを超えて低下、in-band ASD(センサ熱雑音帯)はほぼ無風で「フロアはセンサ律速」の既存結論と整合、日中の絶対閾値スパイク回数(≥0.5/1.0gal)は固定前は毎日>0だったのが固定後2日は0回で「生活振動の鋭いやつが減った」というユーザーの体感を裏付けた。一方夜間窓はスパイクがむしろ増える方向(n=1、未確証)で符号が逆、データが溜まったら再検証が必要。副産物としてS3生波形をobject key単位でローカルミラーするキャッシュ(`.s3cache/`)を作った | [log/2026-08-11-device1-block-fixation-noise-before-after.md](log/2026-08-11-device1-block-fixation-noise-before-after.md) |
| 2026-08-11 | **watchdogに`watchdog_muted`フラグを足し、デバイス単位で欠測監視を止められるようにした。** `namazu-devices`にフラグを立てるとwatchdogが評価そのものをスキップし、ingestが次にバッチを受信すると自動でunmuteされる。試験機(device 4294967295)を試験のたび繋いでは再送スパムに悩まされていた問題への対応で、`docs/STATUS.md`にあった「退役デバイスの扱い」の残タスクも同時に解消した。`tools/mute_device.py mute/unmute/list`で操作。ダッシュボードにも`watchdog_muted`をAPIレスポンスへ足し、muted中は状態表示を「欠測」ではなく灰色「監視停止」にして本当の欠測と区別できるようにした（ラベル文言はユーザーに複数案から選んでもらい「監視停止」に決定） | [log/2026-08-11-watchdog-mute.md](log/2026-08-11-watchdog-mute.md) |
| 2026-08-11 | **`namazu-events`テーブルとS3`events/`に、実行者・AIが直接AWS CLIで誤って消すコマンドを叩く事故への防御を追加した。** DynamoDBに`deletion_protection_enabled`(DeleteTable自体を拒否)とPITR、S3バケットポリシーで`events/*`への削除系操作をDeny、加えてバケット全体にversioning。レビューで「S3 Lifecycle expirationはDenyポリシーを迂回する」既知の穴を指摘され、`expire-raw`ルールのfilterが将来壊れた時の発見・復旧猶予として`noncurrent_version_expiration`を30日に設定。その分`raw_retention_days`を90→60に短縮（実体の存在期間の合計は90日のままでストレージ量は変えず、アプリから見える寿命だけ短縮）。90日という数字自体、`terraform`導入時(2026-07-12、今日でまだ丸30日)に検証無しで決めていたと判明したため見直した。terraform apply実施済み、AWS側の実値（DeletionProtection/PITR/versioning/bucket policy/lifecycle）も確認済み | [log/2026-08-11-events-accidental-deletion-protection.md](log/2026-08-11-events-accidental-deletion-protection.md) |
| 2026-08-11 | **WiFi認証情報をOTA経由で更新する作戦を検討した（未着手・設計のみ）。** 新SSID/パスワードを暗号化した値を公開URLのfirmware.bin自体に埋め込んで配る設計——バイナリが認証なし公開URLである以上、暗号化に実質的な意味がある。暗号方式はXORではなくAEAD（過去バージョンが収集可能な公開バイナリで鍵を使い回すと解析されるため）、鍵は`tools/devices.json`のHMAC鍵と同じくper deviceの方針。「新WiFiに繋がらず詰む」問題には、ファーム更新の成否とWiFi切り替えの成否を分離し、接続試行に成功して初めてNVSへ確定書き込みする設計で対応する方針とし、失敗時は旧設定のまま動き続けるだけでは運用者が気付けないため通知する方針も足したが、`fw_version`突き合わせだけのOTA停滞検知(§2.8)とは違い切り替え失敗時も`fw_version`は目標と一致してしまうため、専用の自己申告フィールドが要ると気付いた | [log/2026-08-11-wifi-credential-ota-rotation-plan.md](log/2026-08-11-wifi-credential-ota-rotation-plan.md) |
| 2026-08-11 | **`docs/STATUS.md`に「デバイス一覧」節を追加し、1号機・2号機・テスト機(device_id=4294967295)の素性を一箇所にまとめた。** それまで各機の情報は`tools/devices.json`（gitignore対象）と`docs/log/`の断片ログにしか無かった | [log/2026-08-11-devices-list-doc.md](log/2026-08-11-devices-list-doc.md) |
| 2026-08-11 | **デバイス詳細ページに「起動時刻」行を追加した。** `boot_epoch_us`はAPIレスポンスに既に入っていたが画面に出ておらず、見る側が「現在時刻－稼働時間」を頭で計算する必要があった。`最終受信`行と同じ`toLocaleString('ja-JP')`表示。本番API(device0001)で日時が正しく出ることを確認済み。ついでに`docs/uptime.md`の「実機・本番デプロイはまだ」という記述が2026-08-07〜08-09のロールアウト実績と食い違っていたのを直した | [log/2026-08-11-device-detail-boot-time-display.md](log/2026-08-11-device-detail-boot-time-display.md) |
| 2026-08-11 | **イベント詳細API(`GET /event?id=`)がテスト機(device_id=4294967295, uint32最大値)のevent_idを400で拒否する不具合を修正した。** `lambda/api/handler.py`の書式検証`\d{4}-\d{1,16}`がdevice_id部分をちょうど4桁に限定していたが、`event_id()`は`:04d`で「最低4桁」ゼロ埋めするだけなので5桁以上は素通りせず400になっていた。デバイス詳細ルートは同種の問題を先に`\d{1,10}`へ直してあったのに`_event()`だけ直っていなかった。`\d{4,10}-\d{1,16}`へ修正し回帰テストを追加 | [log/2026-08-11-event-api-rejects-uint32-max-device-id.md](log/2026-08-11-event-api-rejects-uint32-max-device-id.md) |
| 2026-08-11 | **`batch-uplink` PR #22をマージし`v2.13.0`をタグ付け、`firmware/platformio.ini`・`terraform/build_lambda.sh`のpinを揃えて更新した。** `pio run`3env・`test/run.sh`確認済み。device2(ADXL355機)はバッチ周期15秒が元々プール枯渇に近い機体で、今回の修正の恩恵は大きいと見ているが実機未確認・投入保留のまま | [log/2026-08-11-batch-uplink-v2.13.0-pin-bump.md](log/2026-08-11-batch-uplink-v2.13.0-pin-bump.md) |
| 2026-08-11 | **WiFi遮断の実機試験で、送信が連続失敗しRAMもBatchプールも満杯になると、退避トリガー(`enqueue()`頼み)が二度と来ず`newBatch stuck`が延々回復しない飢餓状態を発見した。** `Uploader::pump()`に自発的`flushToSpill()`を追加して対処([PR #22](https://github.com/nna774/batch-uplink/pull/22)へ追加コミット)。当初「バックオフ上限(60秒)到達時」で実装したが、プール枯渇中はsamplingTaskが生サンプルを即座に捨て続けるため60秒待ちは遅すぎると判明し、`ram_.size() >= maxRam_`という毎`pump()`呼び出し(~50ms)でチェックできる信号へ差し替えた。firmware側の変更は不要。実機での回復確認はまだ | [log/2026-08-11-uploader-starvation-under-sustained-send-failure.md](log/2026-08-11-uploader-starvation-under-sustained-send-failure.md) |
| 2026-08-11 | **タスク分割版(`batchDrainTask`+送信タスク)とmutex化したUploaderをテスト機(FakeSensor)へ実際に焼き、平常時の動作(enqueue→ram_→pump→POST成功のサイクル、約30秒周期)をシリアルログで確認した。** クラッシュ・再起動ループなし。WiFi瞬断シナリオ(本来の目的)の実機確認はまだ | [log/2026-08-11-uploader-task-split-realhw-check.md](log/2026-08-11-uploader-task-split-realhw-check.md) |
| 2026-08-11 | **`uploaderTask`を「吸い出し」（`gBatchQueue`→`Uploader`）と「送信」（WiFi再接続・`pump()`・OTA/再起動判定）の2タスクに分割し、送信側が長時間ブロックしても`gBatchQueue`（深さ4・drop-oldest）が溢れなくなるようにした。** `batch-uplink`側([PR #22](https://github.com/nna774/batch-uplink/pull/22)、未マージ)に`Uploader`内部mutexを追加し2タスクからの並行呼び出しを安全化、`pump()`のネットワークI/O区間はロック対象外にして吸い出し側を巻き込まないようにした。firmwareが直接spillを書く案・常時spillへ直行する案はどちらも検討のうえ見送った(flash摩耗は却下理由にならないと試算で確認済み)。`pio run`3env・`test/run.sh`（firmware/batch-uplinkとも）確認済み。**batch-uplink PRのマージ・タグ付け・firmware側pin更新・実機投入はまだ** | [log/2026-08-11-uploader-task-split-design.md](log/2026-08-11-uploader-task-split-design.md) |
| 2026-08-11 | **テスト機(2号機プロファイル、`kMaxRamBatches=2`込み)で意図的なネットワーク切断→復帰試験を行い、断片化崩壊・クラッシュ無しで安全に乗り切れることを確認した。** 切断中に`newBatch stuck`(最大1195連続fail)は起きたが`maxblock_8bit`は20468〜27636の健全な範囲を維持、復帰後はspillCountが13→1まで単調に排出されPOSTも全て成功に戻った。PR #81のレビュー・マージ待ち | [log/2026-08-11-device2-profile-disconnect-recovery-test.md](log/2026-08-11-device2-profile-disconnect-recovery-test.md) |
| 2026-08-11 | **2号機の`kMaxRamBatches`を3→2(1号機と同じ値)へ下げた。** テスト機で2号機と同じ設定(`kBatchSeconds=15`・`kMaxRamBatches=3`)にPR #72のfix#1/#2込みで焼いたところ、`heap_free`は潤沢なのに`maxblock_8bit`が1396〜1972まで落ち込みDNS解決が繰り返し失敗する断片化を実機再現した。fix#1/#2はヒープ破壊は防ぐが、`kBatchPoolSlots`(=`kMaxRamBatches+1`)ぶんの静的プール確保自体が1号機より1本(18KB)多いという「そもそも空きが足りない」問題までは解決しないと判明。`2`へ下げたところ同条件でDNS失敗0件・`maxblock_8bit` 23540で安定・POST全成功に改善した | [log/2026-08-11-device2-ram-batches-reduction.md](log/2026-08-11-device2-ram-batches-reduction.md) |
| 2026-08-11 | **2号機と同じバッチ設定(`kBatchSeconds=15`・`kMaxRamBatches=3`・レコード12B)を実ADXL355無しで再現するenv`fake-sensor-device2-profile`を追加した。** `config.h`のバッチ設定と`main.cpp`のセンサ選択が別々の`#ifdef`（前者は`NAMZ_SENSOR_ADXL355`単独判定、後者は`NAMZ_SENSOR_FAKE`優先の`elif`）なので、両方を同時定義すればFakeSensor駆動のまま2号機のタイミングだけ再現できると気付いた。PR #72の修正一式を2号機へ投入する前に、テスト機での長時間backlog試験から始める（`docs/log/2026-08-11-device2-upgrade-concern.md`）の次の一歩 | [log/2026-08-11-fake-sensor-device2-profile-env.md](log/2026-08-11-fake-sensor-device2-profile-env.md) |
| 2026-08-11 | **未送信バックログ件数(spill=LittleFS退避済み・ram=RAMキュー内)を毎バッチヘッダ(`X-Namz-Spill-Count`/`X-Namz-Ram-Queued`)で送るようにした。** これまでOLED表示にしか出ていなかった滞留量を、ヒープ空き容量と同じ設計(CloudWatchカスタムメトリクス、詳細ページ限定で最新値取得)でサーバ側からも見えるようにした。dashboardのデバイス詳細画面に「未送信バックログ」行を追加 | [log/2026-08-11-backlog-headers.md](log/2026-08-11-backlog-headers.md) |
| 2026-08-11 | **電源断等でLittleFSに0バイト/途中で切れた退避ファイルができると、`pump()`が同じ壊れたファイルを永遠にリトライし続けキュー全体が詰まる実機バグに対処した。** `batch-uplink`に`discardSpillOn400`オプション(既定false)を追加(`v2.12.0`)し、ingestが実際にHTTP 400（=サーバがボディを構造的に不正と判定）を返した回だけ、その退避ファイルは二度と成功しないとみなして即座に破棄するようにした。`Uploader`はワイヤ形式を知らない設計のまま、0バイトと途中で切れたbinの両方をHTTP 400という単一シグナルで一律に拾える。`main.cpp`でオプトインし、`firmware/platformio.ini`・`terraform/build_lambda.sh`のpinを揃えた。実機での自動削除確認はまだ | [log/2026-08-11-spill-quarantine-on-400.md](log/2026-08-11-spill-quarantine-on-400.md) |
| 2026-08-11 | **デバイス詳細画面の「データ鮮度」「稼働時間」に、丸め表記(`2時間`)へ実秒数を括弧書きで併記する(`2時間(7328秒)`)ようにした。** 一覧テーブルは列幅が厳しいため丸め表記のまま維持し、詳細画面のみ新関数`fmtAgoExact`に差し替えた | [log/2026-08-11-device-detail-exact-seconds.md](log/2026-08-11-device-detail-exact-seconds.md) |
| 2026-08-11 | **ダッシュボードのデバイス版数リンクが`-dirty`付き版数（例`09d6dc1-dirty`）で404になっていたのを直した。** `get_fw_version.py`は作業ツリーが汚れているとgit短縮hashに`-dirty`を付けるが、`fwVersionHtml()`がそれをそのままGitHubのコミットURLに使っていたため、そんなコミットは存在せず404だった。リンク先だけ`-dirty`を削り、表示テキストは付けたまま残した | [log/2026-08-11-dashboard-fw-version-dirty-link-404.md](log/2026-08-11-dashboard-fw-version-dirty-link-404.md) |
| 2026-08-11 | **2号機(ADXL355)は今回のBatchプール枯渇修正一式(fix #1/#2)を未反映のまま(稼働中版数`ba23fb3`、TlsMemPool以降の全変更より前)と確認し、投入は保留にした。** 2号機が実運用で「毎回復帰できている」実績は、当時の`kMaxRamBatches`が1号機6・2号機3(RAMキュー最大占有量108KB/54KB)という差によるものだった(1号機は後に2へ縮小済み)、という当時の仮説を`ba23fb3`のconfig.hで再確認。一方、2号機はバッチ周期15秒がDNS詰まりのブロック時間(14〜20秒)と同程度以下のため、今回の修正一式を焼いた場合`[sampling] newBatch stuck`(安全な欠測)が1号機より高頻度に出る可能性があるという未検証の懸念を記録した。テスト機で様子を見てから2号機へ展開する方針 | [log/2026-08-11-device2-upgrade-concern.md](log/2026-08-11-device2-upgrade-concern.md) |
| 2026-08-11 | **spillに大量backlog(140本超)がある間、Batchプール(3スロット)枯渇時の素朴mallocフォールバックがヒープを壊しDNS解決まで巻き添えにしていた根本原因を実機ログで特定した。** 一時的な計装(`heap-diag`タグ、コミットしない使い捨て)で、ヒープの段差が`newPooledBatch()`のフォールバック直後に起きることを直接確認。この機体は`MALLOC_CAP_8BIT`の最大連続ブロックが17396バイトで頭打ち(PR #54と同じ値)で、フォールバックが要求する18064バイトを常に下回るため、フォールバックはほぼ確実に失敗し続け、既に生サンプルの欠測(2989回のプール枯渇・最長15秒の連続欠測)を無駄なmalloc試行付きで起こしていたと判明。対応として(1)フォールバックを削除しプール枯渇時は素直にサンプルを諦める、(2)`Uploader::pump()`のRAM優先送信化(`ram_`が慢性的に上限で張り付くのを防ぐ、batch-uplink v2.11.0)の2点を実装し、実機で同条件から枯渇0回・欠測0回・送信失敗0回・heap_free/maxblock_8bit完全横ばいへ改善したことを確認した | [log/2026-08-11-batch-pool-fallback-heap-corruption.md](log/2026-08-11-batch-pool-fallback-heap-corruption.md) |
| 2026-08-11 | **`firmware/lib/Shindo`の内部配列（FIR履歴`hist_`・60秒移動窓`composite_`）をfloatからint16固定小数点へ落とし、静的RAMを約15KB削減した（実機`pio run`のRAM使用量78180B→63116B）。** `hist_`はスケール共通(0.1gal刻み)で無条件に安全だが、`composite_`は同じ丸めだと弱い揺れ(震度0.5前後)でアラート閾値の跨ぎ判定が稀にズレるとホストg++でのfloat版突き合わせで判明し、`composite_`だけ分解能を上げた専用スケール(kCompositeScale=28、震度7超をカバー)に分離した。残るジッタは分解能を上げても消えない量子化の宿命だが、クラウド側detect Lambdaが生データから独立に同じ閾値で再評価するため二重のセーフティネットがあると判断し許容。実機投入はまだ | [log/2026-08-11-shindo-int16-compression.md](log/2026-08-11-shindo-int16-compression.md) |
| 2026-08-10 | **`Uploader::pump()`のspill読み込み用`malloc(18032)`(TLS・Batchバッファに続く3つ目の断片化源)を固定バッファ化し(`batch-uplink` v2.10.0)、実機でmalloc失敗が完全に消えたことを確認した。ただしBatchプール(3スロット)枯渇時の一般mallocフォールバックは未対応のまま残っており、`maxblock_8bit`が低い局面で新規バッチ組み立てが数秒〜10秒超連続失敗する現象を実機で直接観測した。** `[sampling] newBatch stuck`診断ログをsamplingTaskに追加、`POST failed (connection refused)`もこの断片化と相関している可能性が高いと見ている(未確定)。`gBatchQueue`満杯drop・spill満杯dropはどちらも今回の試験(spillCount最大22本)では発火せず、キュー投入済みバッチの喪失は確認されなかった。device1/device2投入はまだ | [log/2026-08-10-spill-read-fixed-buffer.md](log/2026-08-10-spill-read-fixed-buffer.md) |
| 2026-08-10 | **pull型OTAが`TlsMemPool`の「TLS接続は同時に1本」前提を破りうると気付き、`batch-uplink`に`Uploader::closeConnection()`(v2.9.0)を足して`pauseSamplingForOta()`から呼ぶよう修正、実機(予備基板)で修正前は1回目ほぼ確実に競合(`calloc FAILED`)・修正後は1回目から確実に回避、を両方確認した。** `checkAndPerformPullOta()`がバッチPOST成功と同じ周で発火する構造上、修正前は初回がほぼ確実に負け(2回目以降はidle接続クローズのタイミング次第で通る非決定的な挙動)だったが、修正版は`closeConnection()`を能動的に呼ぶことでこの巡り合わせに依存せず初回から通ることを実機で確認した。device1/device2への投入判断はまだ | [log/2026-08-10-ota-tls-pool-race.md](log/2026-08-10-ota-tls-pool-race.md) |
| 2026-08-10 | **TlsMemPoolのoutstanding会計ドリフトを修正した——`poolCalloc`の加算をブロックの実サイズ(`headerOf(p)->size`)基準に揃えて`poolFree`の減算と対称化し、`poolFree`にO(1)の防御チェック(サイズ範囲チェック・outstanding負値化の即時警告)を追加、アロケータの純粋ロジックを`TlsMemPoolCore`へ切り出してホストg++の乱数ストレステスト(3万回calloc/free、分割スキップ境界の狙い撃ちケース含む)を新設した。** 修正前のロジックに戻すと同じテストが一発で(stressケースでは-59411まで)落ちることを確認し、リグレッション検出力を裏付けた。`pio run`3env・`firmware/test/run.sh`・pytest169件確認済み。実機投入はまだ | [log/2026-08-10-tls-pool-outstanding-accounting-fix.md](log/2026-08-10-tls-pool-outstanding-accounting-fix.md) |
| 2026-08-10 | **TlsMemPoolの`sCurrentOutstanding`が実機で大きく負値化した(call #353147で-1073132)原因を特定した——`poolCalloc`が加算する生の要求バイト数と、`poolFree`が減算するブロックヘッダの実サイズ(アライン切り上げ・分割スキップ時は更に大きい)が非対称で、alloc/freeのたびに統計が負側へドリフトしていた。** ブロックのリンクリスト自体(`BlockHeader`のmagic/size/physNext/physPrev、`mergeWithNext`の結合順序)は監査した限り破損なし。「単一のuploaderTaskからしか呼ばれない」という設計前提も`main.cpp`確認済みで現状は崩れていない。実害は統計カウンタのみで、確保領域が重なる静かなヒープ破壊の根拠は無し。修正(加算/減算の対称化・O(1)防御チェック・ホストストレステスト)は次のログで着手 | [log/2026-08-10-tls-pool-outstanding-accounting-drift.md](log/2026-08-10-tls-pool-outstanding-accounting-drift.md) |
| 2026-08-10 | **RAMキューに積まれる`Batch`本体(約18KB)も、TlsMemPoolと同じ「専用固定プール化」でmalloc(18032)断片化詰まりから隔離した。** batch-uplink PR #13(既にv2.4.0でマージ済み、現行pin v2.8.0に含まれる)の外部バッファ+解放コールバック版`Batch`コンストラクタを使用。同じ実験を今日別の並行セッションが既に実機で試み、スロット数見積もり(組み立て中1+`gBatchQueue`深さ4+`kMaxRamBatches`=当時7本126448B)が大きすぎてsamplingTask生成失敗によるパニック再起動ループを踏み一旦見送っていた（`docs/log/2026-08-10-newbatch-buffer-pool-handoff.md`）。今回はスロット数を`kMaxRamBatches+1`(3本54192B)に絞り、`xTaskCreatePinnedToCore()`の戻り値チェック漏れ(前回のクラッシュの真因)も修正。実機(fake-sensor)で起動成功・プールmalloc成功(free_heap=159324, maxblock_8bit=57332)・タスク生成失敗なし・通常運転14サイクル(約7分)連続送信成功、heap_free/maxblockとも横ばいを確認。**ただし確認できたのはspill=0の定常状態のみ。これまで問題が起きていたのはspillに大量の未送信分が溜まった状態からの復旧局面で、そこはWiFi遮断が要るため未検証のまま残っている** | [log/2026-08-10-batch-ram-pool.md](log/2026-08-10-batch-ram-pool.md) |
| 2026-08-10 | **`malloc(18032)`が延々失敗し続ける原因を`MALLOC_CAP_8BIT`側の断片化(要求18032に対し実測17396、636バイト不足)と実機で確定し、device1/device2の非対称性(欠測からの復帰可否)を`kMaxRamBatches`の差(108KB/54KB)で説明できるとユーザーの実運用経験と一致することも確認した。** `ESP.getMaxAllocHeap()`(`MALLOC_CAP_INTERNAL`基準)は`heap_free`潤沢・断片化なしと過大報告し続けており、実際に`malloc()`が使う`MALLOC_CAP_8BIT`側の最大連続ブロックだけがこの機体で頭打ちになっていた。TlsMemPool(52KiB)導入後も残っていた壁で、`kMaxRamBatches`縮小(現在は保留)が本命の対策候補として残っている。実装は無し、調査ログのみ | [log/2026-08-10-tls-dedicated-pool.md](log/2026-08-10-tls-dedicated-pool.md) |
| 2026-08-10 | **`newBatch()`が繰り返し失敗する「無期限に無応答」バグの正体を実機で特定し、mbedTLS専用固定プール(52KiB)を実装した。** 実測プローブでTLSハンドシェイクのピーク同時確保量(48304B)を測定、`batch-uplink`にタイミングログ(v2.5.0)を追加して原因を特定——ソケットのブロッキングではなく、TLSセッションが握ったまま解放しない約41.8KBに邪魔されて同じ退避ファイルの`malloc(18032)`が指数バックオフ上限(60秒)で永遠に失敗し続けるだけの、正常だが検知不能なリトライループだった。`firmware/lib/TlsMemPool`で`mbedtls_platform_set_calloc_free()`によりTLSの確保/解放を専用52KiBプールへ隔離、プールが尽きてもcalloc失敗として安全に縮退し一般ヒープを奪わない設計。実機で狙った効果(以前一度も通らなかった`malloc(18032)`が成功)を確認済み。`pio run`3env・`test/run.sh`確認。**実機(device1/device2)投入判断はまだ** | [log/2026-08-10-tls-mem-pool.md](log/2026-08-10-tls-mem-pool.md) |
| 2026-08-10 | **`batch-uplink`のpinをv2.4.0→v2.8.0へ更新した。** device1調査で積んだ4つの変更(v2.5.0:`pump()`各段階のデバッグログ、v2.6.0:ヒープ枯渇時`loadOldestSpillPath()`の未捕捉例外abortをtry/catchで安全化、v2.7.0:`heap_free`/`maxblock`ログ、v2.8.0:`MALLOC_CAP_8BIT`側の実測値ログ)を取り込む。いずれも`batch-uplink`側で既にマージ・タグ付け済みで、firmware側のpin更新のみ。firmwareのコード変更は無い。`pio run`3env・`test/run.sh`確認済み | [log/2026-08-10-batch-uplink-v2.8.0-pin-bump.md](log/2026-08-10-batch-uplink-v2.8.0-pin-bump.md) |
| 2026-08-10 | **`loop()`(Core1)と`uploaderTask`(Core0)がロックなしでLittleFSへ同時アクセスしていたクラッシュ(`lfs_file_close`のassert失敗)を実機で発見・修正した。** `main.cpp`の`loop()`がディスプレイ更新用に250msごと呼ぶ`Uploader::oldestQueuedStartUs()`が、`pump()`と同じLittleFSのspillへ別コアから同時アクセスしていたのが原因。以前からある独立のバグで、高頻度リトライで競合窓が広がり顕在化した。`Uploader`へmutexを持ち込む代わりに、`gDispIntensity`等と同じ「1書き手・1読み手のvolatile」パターンを踏襲し、`uploaderTask`側で計算した値を`gBacklogAgeS`へpublish、`loop()`は読むだけにしてLittleFSアクセスをCore0へ一本化。実機で10分間の連続監視でクラッシュ0件を確認。`pio run`3env・`test/run.sh`確認済み | [log/2026-08-10-littlefs-loop-uploader-race.md](log/2026-08-10-littlefs-loop-uploader-race.md) |
| 2026-08-10 | **`newBatch()`失敗対策の次善策(mbedTLS専用固定プール化、design.md予備案)に向け、実機でTLSの確保サイズ・同時確保量を測るプローブを用意した。** `mbedtls_platform_set_calloc_free()`が実際にこのSDK(ESP-IDF側の`esp_config.h`)で有効になっていることを確認した上で、calloc/freeを横取りして呼び出し回数・合計/同時確保バイト数・最大単発サイズを追跡する`firmware/lib/TlsAllocProbe`と専用env(`tls-alloc-probe`、`env:fake-sensor`を拡張し実際のingest/alert TLS送信で測る)を追加。本番機のバイナリには含めない。`pio run`4env・`test/run.sh`確認済み | [log/2026-08-10-tls-alloc-probe.md](log/2026-08-10-tls-alloc-probe.md) |
| 2026-08-10 | **`loop()`の画面更新が250msごとに行っていたString連結（毎回ヒープをalloc/free）を固定バッファ+snprintfに置き換えた。** 「無駄遣い調査」第3弾（第1弾はShindoの静的24KBバッファ、第2弾はLAN内push型OTA撤去）。前2つは静的RAM、今回は動的ヒープの継続churnが対象——`status`/`clock`/`ip`の3文字列とDisplayライブラリの公開API(`render`等の引数、`lastClass_`)をString→固定char配列に揃えた。`WiFi.localIP().toString()`もIPAddressの`operator[]`直読みに替え経路ごと排除。断片化への実際の寄与は未実測（仮説段階）。静的RAMはほぼ不変（82092B→82084B、狙い通り——今回の目的はalloc/free回数削減であって静的サイズ削減ではない）。`pio run`全4env・`test/run.sh`確認済み。実機投入・断片化改善効果の検証はまだ | [log/2026-08-10-display-string-heap-churn.md](log/2026-08-10-display-string-heap-churn.md) |
| 2026-08-10 | **使っていないLAN内push型OTA(ArduinoOTA)を全撤去し、静的RAMを106092B→102132B(約3.9KB減)・Flashを約39KB減らした。** VLAN分離でespota転送が一度も届かず放置されていた機能。「無駄遣いしてるとこないか」調査でユーザーに確認し、削減量次第で撤去してよいと条件付き承認を得たうえで実測してから全部消す方針を確定。`main.cpp`のコードだけでなく`platformio.ini`の`-ota` env・`DeviceIdentity`のotaPassword(NVS)・`tools/provision_device.py`のota-passwordコマンド・`docs/ota.md`（全面書き直し、§7だったpull型が§2に）まで一式削除。pull型OTAと共有の安全停止シーケンス(`pauseSamplingForOta`等)はそのまま残した。`pio run`全4env・`test/run.sh`・pytest169件確認済み | [log/2026-08-10-drop-lan-push-ota.md](log/2026-08-10-drop-lan-push-ota.md) |
| 2026-08-10 | **`Shindo::currentIntensity()`が毎回確保していた静的24KBの作業バッファ(`tmp[6000]`)を、サイズ30の最小ヒープに置き換えて撤去し、静的RAM使用量を106092B→82092Bへ減らした。** `newBatch()`失敗対策のバッファプール化がDRAM予算の壁（残り約18.5KB）で見送りになった件を受け、ユーザーの依頼で`nm --size-sort`のシンボル一覧を洗って発見。移動窓`composite_`のコピーを取らず`nth_element`で直接並び替える案は、循環バッファの「次の上書き位置=最古」という不変条件を壊すため実測で不可と確認し、composite_を読み取り専用のまま上位30個の最小値だけを最小ヒープで求める方式に落ち着いた。ホストg++で旧実装との出力突き合わせ（複数パターン計35000サンプル）で一致を確認。`pio run`全env・`test/run.sh`確認済み。プール化の再検討・実機投入はまだ | [log/2026-08-10-shindo-currentintensity-heap-tmp-buffer-removal.md](log/2026-08-10-shindo-currentintensity-heap-tmp-buffer-removal.md) |
| 2026-08-10 | **FakeSensor(結合試験用)のsensor_type=255に「ダミー」という表示名を足した。** 4294967295番機の詳細ページで「センサ」欄が`不明`だったのを見たユーザーから「何か送ってるんだっけ」と聞かれ、DynamoDBを直接引くと実際に`sensor_type: 255`（FakeSensorが送る意図的なsentinel値）が記録されていたと判明。「未記録」と「意図的なダミー」が区別できていなかった不具合。`wire.SENSOR_TYPE_NAMES`に追加するだけで直る、単一の真実の恩恵。pytest 113件通過 | [log/2026-08-10-fake-sensor-dummy-label.md](log/2026-08-10-fake-sensor-dummy-label.md) |
| 2026-08-10 | **デバイス詳細API(`/devices/<id>`・`/devices/<id>/temp`)のルーティング正規表現が`\d{1,4}`(4桁まで)しか受け付けず、テスト用device_id `4294967295`(uint32最大値、10桁)の詳細ページが一覧には出るのに404になっていたのを`\d{1,10}`に広げて直した。** 一覧側(`_devices()`)はパスにIDを埋め込まないためこの制限を踏まず、食い違いに気付きにくい形の不具合だった。回帰防止に`handler()`を直接叩くテストを追加、pytest 112件通過 | [log/2026-08-10-device-detail-404-for-large-device-id.md](log/2026-08-10-device-detail-404-for-large-device-id.md) |
| 2026-08-10 | **watchdogのSlack通知（欠測・データ遅延・pull型OTA停滞とその復帰）すべてに、ダッシュボードのデバイス詳細ページ`#device/<id>`へのリンクを足した。** `batch-uplink`の`notify.py`にある`event_url`/`event_field`と同じパターンだが、pin(`v1.8.0`)とmaster(`v2.4.0`)のギャップに無関係な変更を巻き込まないよう、共有ライブラリは変更せず`lambda/watchdog/handler.py`にローカルヘルパー`_device_field()`を置いた。pytest 111件通過 | [log/2026-08-10-watchdog-slack-device-link.md](log/2026-08-10-watchdog-slack-device-link.md) |
| 2026-08-10 | **PR #54のマージ前レビューで、バッファプール見送りの`main.cpp`一括revert(下記2行下)がtask watchdog 10秒→20秒化(2026-08-09にDNS解決14秒対策として実機検証済み)まで巻き添えで消していたと発覚、復元した。** revertは「プール導入前の状態へ戻す」を`main.cpp`/`config.h`丸ごとのorigin/master復元で行ったため、同じファイル内にあった無関係な既検証の修正まで一緒に消えていた。`docs/design.md`は「実機で確認した」と書いたままだったため、ドキュメントと実コードが食い違ったまま気づかず進みかけた。コメント含め該当コミット(db78cec)の内容をそのまま復元、`pio run -e esp32dev -e adxl355`・`firmware/test/run.sh`で確認。**教訓: 複数の変更が同じファイルに同居する状態で一部だけ取り消す時は、diffで対象を確認してから戻すこと** | [log/2026-08-09-device1-outage-reboot-loop-and-data-loss.md](log/2026-08-09-device1-outage-reboot-loop-and-data-loss.md) |
| 2026-08-10 | **newBatch()失敗対策のバッファプール化を、予備基板の結合試験で問題が続けて見つかり一旦見送った。** DRAM予算の壁は`malloc()`一発確保で越えられたが(前回行)、実際にFakeSensorとテスト用device_id(UINT32_MAX、terraform applyでサーバ側にも登録)で本番ingest経路まで通す結合試験をしたところ、6スロットはsamplingTaskのタスク生成に必要なヒープを食い潰しパニックループ、5スロットに削るとクラッシュは止まるが`kMaxRamBatches=0`がUploaderの未定義動作を踏み、`kMaxRamBatches=1`まで戻しても送信(`pump()`)が進まない原因不明の停滞が残った。「この機体の8bit可能DRAM予算自体が限界に近く、何を積んでも他を圧迫する」構造的な問題と判断し、`main.cpp`/`config.h`を`origin/master`の状態(プール導入前)へ完全に戻した。`batch-uplink`のPR #13(前方互換な追加のみ)は巻き戻さず温存。結合試験用に作った`FakeSensor`/`env:fake-sensor`は汎用ツールとして残した。次はmbedTLS専用プール化かプール化断念かの判断から再開、**device1/device2は一切触れていない** | [log/2026-08-10-newbatch-buffer-pool-handoff.md](log/2026-08-10-newbatch-buffer-pool-handoff.md) |
| 2026-08-10 | **newBatch()失敗対策の固定バッファプール化を、DRAM予算の壁を乗り越えて実現した。** 「静的配列で全スロットぶん確保する」実装が原因でリンカの`dram0_0_seg`制限に当たっていたと判明——`memory.ld`のコメントに「この制限は静的配置専用の保守的な値で、本来のDRAM残りは実行時のヒープでは使える」と明記されていた。`setup()`での`malloc()`一発確保に変えたら制限を受けず、予備基板の実機検証(`env:pool-probe`)で6スロット(108384B、`kMaxRamBatches=1`)の確保成功・7スロット(126448B)の失敗を確認。6スロットを採用してfirmware側を最終化(`sBatchPool`をmalloc()方式へ、`kMaxRamBatches`2→1)、`pio run -e esp32dev -e adxl355`のリンク成功・`firmware/test/run.sh`確認。前回行(下)で「未決着」としていたmbedTLS専用プール化等の代替案は不要になった。**実機(device1/device2)への投入・batch-uplink PR #13のマージ判断はまだ** | [log/2026-08-10-newbatch-buffer-pool-handoff.md](log/2026-08-10-newbatch-buffer-pool-handoff.md) |
| 2026-08-10 | **newBatch()失敗対策の固定バッファプール化を実装しかけ、DRAM予算の壁で頓挫した。** batch-uplinkに外部バッファ+解放コールバック版Batchコンストラクタを実装([PR #13](https://github.com/nna774/batch-uplink/pull/13)、v2.4.0先行タグ)、firmware側にも固定プールを組んだが、複数本ぶんを静的確保しようとしたらリンカが`dram0_0_seg`をオーバーフローさせて即死。実測で`malloc()`が実際に使えるDRAM(8bitアクセス可能領域)の総容量は約124.5KiBしか無く、既存firmwareが約106KB使用済みで残り約18.5KBと判明（前日実測したMALLOC_CAP_INTERNAL/8BITの乖離を裏付ける形になった）。ユーザー提案でPSRAM搭載の可能性も検討し、診断用ビルド(`env:psram-probe`)を用意して予備の同型基板で実機確認したが、PSRAM無しと確定。複数本プールは断念、mbedTLS専用プール化/1スロット妥協/プール化断念(再試行のみ)の3択が未決着のまま残っている | [log/2026-08-10-newbatch-buffer-pool-handoff.md](log/2026-08-10-newbatch-buffer-pool-handoff.md) |
| 2026-08-10 | **`newBatch()`失敗調査を、応答がおかしくなった別セッションから引き継いだ（実装は無し）。** 引き継ぎ元がローカルに残していた未pushコミット（`MALLOC_CAP_INTERNAL`と`MALLOC_CAP_8BIT`の乖離を実測で診断、固定バッファプール化の設計をドラフト）を取り込んだ上で内容を検証し、「確定した」としていた箇所を単発実測に基づく仮説へdesign.mdの表現を弱めた。ユーザーが独立に提案した「事前確保して使い回す」案は同じ方向。当初「所有権契約を変えるとElectabuzzに影響する」を理由に既存ドラフト(`Uploader`の契約は変えず`Batch`のデストラクタ側だけでプール返却)を推したが、batch-uplinkはタグ運用でNamazu側のpinを上げない限りElectabuzzには波及しないというユーザーの指摘で誤りと判明・訂正。既存ドラフトを最初の一手に推す結論自体は「変更が`Batch`一箇所に閉じ診断済みの問題に十分効く」を理由に維持。プールのスロット数見積もりと実装(batch-uplink`external-buffer-batch`ブランチ)は次の課題として持ち帰り | [log/2026-08-10-newbatch-buffer-pool-handoff.md](log/2026-08-10-newbatch-buffer-pool-handoff.md) |
| 2026-08-09 | **device1が再び長時間欠測した件を実機デバッグ計装まで入れて調査し、TASK_WDTパニックの真因を特定・修正した。** S3突き合わせで19:10:45〜21:09:54 JSTの約119分ぶんが実在しないと確認、原因はspill容量ではなく既知の`gBatchQueue`(深さ4)starvationだった。意図的な全断実験で`reset_reason=TASK_WDT`を再現し、`WiFi.hostByName()`内部のDNS解決待ち(lwIP内部タイムアウト14秒)がHTTPClientの接続タイムアウトにもTLSハンドシェイクタイムアウトにも守られていない別枠のブロッキングだと特定。batch-uplinkにハンドシェイクタイムアウト短縮(4秒、v2.2.0、[PR #11](https://github.com/nna774/batch-uplink/pull/11))・`main.cpp`のtask watchdogを10秒→20秒へ拡張し、実機で「14.04秒でDNS失敗→パニックせず正常復帰」を確認した。`gBatchQueue`吸い出しもこの対策だけで正常動作すると確認、2タスク分離の緊急度は下がった。`namzwire::newBatch()`がヒープに十分な空きがあるのに失敗しバッチ形成が止まる謎も、`ESP.getMaxAllocHeap()`(`MALLOC_CAP_INTERNAL`基準)が`malloc()`の実際に使える量(`MALLOC_CAP_8BIT`基準)を過大報告していたと実測で特定した（`maxblock_internal=45044`に対し`maxblock_8bit=17396`、必要な18038に642バイト不足）。対策として`Batch`に外部バッファ+解放コールバックを足し固定バッファプールで使い回す設計まで固めたが、実装はまだ（batch-uplink`external-buffer-batch`ブランチ、コミット無し）。「単発フリーズだった」という前回結論も一次証言のみの弱い根拠だったと分かり訂正した。自己修復リブートのアイデアをmemoに残した | [log/2026-08-09-device1-outage-reboot-loop-and-data-loss.md](log/2026-08-09-device1-outage-reboot-loop-and-data-loss.md) |
| 2026-08-09 | **ダッシュボードのライブ「1分」表示に複数機の重ね表示モードを追加した。** #52の傾き・相対方位較正値（`namazu-devices`の`tilt_up`/`azimuth_deg`）を使い、較正済みの機を2台以上選ぶとUD＋回転済み水平2軸を重ねて描く。実装前にユーザーへ「重ねる中身(UD+回転水平2軸を採用)」「UI配置(既存ライブ画面にトグル)」を確認し、追加で「`overlay=1,2`のようにURLへ持たせられるか」の要望を受けて2台限定ではない一般的な作りにした。実装中、`fit_relative_azimuth`の回転方向をdocstringの文言だけで実装したら符号が逆だったため、Pythonの実装を実際に検算して修正（`Rot(-azimuth_deg)`が正しい）。api(`/devices`)に較正値を追加、回転・共通グリッドへの補間・描画はすべてクライアント側。pytest169件・`node --check`確認、モックAPIでの合成データ（既知の較正値から生成）をヘッドレスChromeでフルパイプライン確認。本番デプロイ・実機での最終確認はまだ | [log/2026-08-09-live-overlay-mode.md](log/2026-08-09-live-overlay-mode.md) |
| 2026-08-09 | **1号機をブロックへ剛結し、机を叩いて傾き・相対方位を較正した。** 静穏区間の重力DCから傾き(1号機0.85°／2号機0.47°、どちらもほぼ水平)、タップ直後±0.2〜0.5秒の水平粒子運動の主軸フィット(ラグ探索つきProcrustes回転、コヒーレンス0.98)から相対方位(2号機を-7.9°回すと1号機に一致)を求める`tools/calibrate_orientation.py`を新規作成し、`namazu-devices`へ書き込んだ。絶対方位(真の北)はまだ未定、相対値のみ | [log/2026-08-09-orientation-calibration.md](log/2026-08-09-orientation-calibration.md) |
| 2026-08-09 | **IIS3DHHC温度換算式の桁ズレ(raw/16+25→raw/256+25)を実機投入直後に発見・訂正した。** STの公式ドライバ`iis3dhhc_from_lsb_to_celsius()`(`raw/16+25`)は12bit値を渡す前提で、16bit生値をそのまま渡すと16倍過大になる（実機で201℃という値が出て発覚）。データシート(DS12292 §7.6)で16bitが12bit値の左詰め（下位4bit常にパディング0）と確認し訂正。`namazu-device-temp`はraw保存・℃は読み出し時計算なので、Lambda再デプロイのみで直る（ファーム再書き込み不要）。pytest 109件通過 | [log/2026-08-09-iis3dhhc-temp-scale-fix.md](log/2026-08-09-iis3dhhc-temp-scale-fix.md) |
| 2026-08-09 | **1号機(IIS3DHHC)の内蔵温度センサを有効化した。** 「IIS3DHHCは温度センサを持たない」という前提（`dashboard/app.js`のコメントに残っていた）が誤りで、STの公式レジスタ定義(`OUT_TEMP_L/H`, 0x25/0x26, `raw/16+25`)を確認して実装した。1号機を2号機と同じブロックへブレッドボードごと剛結する据え付け案で「自己発熱を2号機の温度読みで代用できないか」と検討した際に発覚。`firmware/lib/Iis3dhhc/`に`readTemperatureRaw()`を追加、`lambda/common/wire.py`に`iis3dhhc_temp_c()`を追加、`dashboard/app.js`の表示条件も直した。pio run・test/run.sh・pytest 109件通過。実機書き込み・デプロイはまだ | [log/2026-08-09-iis3dhhc-embedded-temperature.md](log/2026-08-09-iis3dhhc-embedded-temperature.md) |
| 2026-08-09 | **岩手県沖M5.6(確定報。第1報M4.9・震央距離483km)を正式イベントとしては逃したが、事後解析では拾えていたと確認した（実装は無し）。** `/events`に該当時刻の新規イベントは無く、`firmware`のリアルタイム閾値には未達。S3生波形を`detectlab.py`で事後解析すると予測P波到達窓内にSTA/LTA山が両機で一致(4.32/4.50)。さらにバンドパス後の3軸合成RMSを直接見たところ、**P波到達と同時に振幅が2〜2.5倍へ立ち上がり約160〜180秒かけてなだらかに減衰する、両機に共通する波形**が確認できた（STA/LTA比だけでは急な立ち上がりしか拾えず、なだらかな減衰は見落としていた＝レビューで指摘され訂正）。`docs/noise.md`の過去3例の中でも最も明瞭な**probable detection**と判断。`tools/promote_event.py`で両機とも永久保存（`0001-59540398`/`0002-59540398`）し、同ファイルの「検出できた／できなかった」に事例を追記。副産物として`detectlab.py`の重ね描き図に実時刻の目盛りを焼き込んだ | [log/2026-08-09-iwateoki-m4.9-post-hoc-detection.md](log/2026-08-09-iwateoki-m4.9-post-hoc-detection.md) |
| 2026-08-09 | **reset_reasonテレメトリを本番デプロイし、device2へOTA配信して実機確認した。** terraform apply(4 Lambda更新)・ダッシュボード配信・`tools/publish_ota.sh adxl355`でファーム(ba23fb3)公開・`request_ota.py`で許可、device2が自動更新し`fw_version=ba23fb3`で着地。ダッシュボードで「前回の再起動理由: SW」（OTA自身のESP.restart()による意図的な再起動、想定通り）を確認し、パイプライン全体の疎通が取れた。副産物: worktreeに`.venv`が無いため`tools/publish_ota.sh`のpio解決にPATH前置きが要ること、`.venv`シンボリックリンクだと`.gitignore`のディレクトリパターンが効かずdirty判定されて`-dirty`版を誤って一度公開してしまったこと（`aws s3 rm`で削除・撮り直し済み）を記録 | [log/2026-08-09-reset-reason-deploy-and-device2-ota.md](log/2026-08-09-reset-reason-deploy-and-device2-ota.md) |
| 2026-08-09 | **直前の再起動理由(esp_reset_reason)を送信・記録・表示するようにした。** device1/device2の予期しない再起動をWDT panic説とヒープ枯渇説のどちらか実機データで切り分けるための可観測性。firmwareが起動時に`X-Namz-Reset-Reason`ヘッダで送り、ingestが`boot_epoch_us`と同じUpdateItemに相乗りで記録（追加書き込み無し）、api/dashboardの「稼働時間」の隣に表示。実装中、値をコピーする`kExtraRequestHeaderValues[]`の静的初期化の仕組み上「setup()で`const char*`変数を差し替える」案では反映されないバグに気づき、固定バッファへコピーする方式に直した。sensortest環境のビルドを一瞬壊しかけた(`#ifndef NAMZ_SENSOR_TEST`漏れ)のも`pio run`で発見して修正。pio run全4env・test/run.sh・pytest(106件)確認済み。terraform apply・実機OTA配信はまだ | [log/2026-08-09-reset-reason-telemetry.md](log/2026-08-09-reset-reason-telemetry.md) |
| 2026-08-09 | **batch-uplinkのヘッダ配列(watchResponseHeaders/extraRequestHeaderNames+Values)をnullptr終端方式に作り直し、本数上限(kMaxWatchedHeaders/kMaxExtraRequestHeaders=4)を撤去してv2.0.0を切った。** device2の再起動調査でreset_reasonヘッダを足そうとしたら4/4使い切っていると判明し、上限の由来を調べたところハードウェア制約ではなく「対称設計」で揃えただけの数字と分かった。当初の4→5拡張案(v1.9.0)から、count引数自体を無くすargv方式へ倒し、破壊的変更なのでsemver的にv2.0.0が正しいと訂正した。`extraRequestHeaderValues`は生ポインタのまま維持（コピー所有に変えるとuptime/heapが起動時の値に固定される回帰になるため）。firmware/platformio.ini・terraform/build_lambda.shのpinをv2.0.0へ揃え、main.cppの呼び出し側をnullptr終端に追従。pio run(esp32dev/adxl355)・test/run.sh・pytest(104件)確認済み。Electabuzzはタグpinのため無影響。terraform apply・実機OTA配信はまだ | [log/2026-08-09-uplink-v2.0.0-sentinel-header-arrays.md](log/2026-08-09-uplink-v2.0.0-sentinel-header-arrays.md) |
| 2026-08-08 | **heapテレメトリをダッシュボードのデバイス詳細ページから軽く見えるようにした。** チャート化(新規API+チャートコード一式)ではなく最新値のテキスト1行+CloudWatchコンソールへの深リンクを選んでもらった。`common/metrics.latest_heap()`で直近10分の最新点を取得し`/devices/<id>`（単体のみ、一覧は対象外）へ追加、terraformに`cloudwatch:GetMetricStatistics`権限を追加。pytest 104件・terraform validate・`node --check`確認。CloudWatchコンソールへの深リンクURL形式はログインセッションが無く実際の描画は未検証。apply/実機確認はまだ | [log/2026-08-08-heap-telemetry-dashboard-glance.md](log/2026-08-08-heap-telemetry-dashboard-glance.md) |
| 2026-08-08 | **ボタン長押し緊急再起動、確認画面に入る反転を打ち消すようにした。** PR #42配信後の実機再確認で「動作はかなりいいが、今の画面を見たまま押し始めるので確認画面(黄)に入る時点で反転して見えるのが分かりづらい」と指摘。confirm閾値到達時にもう一度`toggleFlip()`し、押し始めの反転を打ち消すようにした（確認画面自体は常に押す前と同じ向きで出る。途中経由の一瞬の反転は許容、というユーザー判断）。キャンセル時もNVSの向き保存が押す前の値に戻るので副作用は残らない。firmware両env成功、実機再確認はまだ | [log/2026-08-08-emergency-reboot-button-flip-cancel.md](log/2026-08-08-emergency-reboot-button-flip-cancel.md) |
| 2026-08-08 | **ボタン長押し緊急再起動、実機testで見つかった3件を直した。** device2への配信後の実機操作確認で: (1)画面反転をrelease edge判定にしていたら離す瞬間の接点バウンスで相殺され効かなくなる不具合があったため、実績のあるpress edge判定に戻した、(2)確認画面(黄)から閾値未満で離してもキャンセルされず画面が固まる不具合を直した(離した時点で`rebootArmed`をfalseに戻す)、(3)confirmed遷移時に背景色が同じでレイアウトだけ変わるため前の文字が重なって見える表示崩れを直した(`paintFrame`を1回強制)。firmware両env・`test/run.sh`成功、実機再確認はまだ | [log/2026-08-08-emergency-reboot-button-field-fixes.md](log/2026-08-08-emergency-reboot-button-field-fixes.md) |
| 2026-08-08 | **ボタン長押し緊急再起動(v=1726fce)をdevice2へOTA配信し、着地・再起動を確認した。** `tools/publish_ota.sh adxl355`→`tools/request_ota.py request 2 1726fce`で配信、約2分後に`fw_version`着地・`boot_epoch_us`更新(`uptime_s`が78.5秒に巻き戻り)・`pending_ota_version`の自動クリアを確認した。着地直後の1回目のポーリングでは`boot_epoch_us`がまだ更新されておらず、再起動検知の閾値(`BOOT_EPOCH_DRIFT_THRESHOLD_US`=±2分)より速く再起動が完了すると一時的に「再起動なし」に見えることが分かった（誤検知ではなく検知の粗さ、実際には既に再起動済みだった）。**物理ボタンでの実際の長押し操作自体の確認はまだ**（現地作業が必要） | [log/2026-08-08-emergency-reboot-button-device2-rollout.md](log/2026-08-08-emergency-reboot-button-device2-rollout.md) |
| 2026-08-08 | **ボタン長押しでの緊急手動再起動を実装した。** device1の再起動をWDT panic仮説（PR #39、未マージ）で調査中に、「通信不調そのものが原因ならリモート再起動は届かない」と気づき、通信に頼らず現地のボタンだけで再起動できる手段を先に用意した。既存の画面反転ボタン(GPIO0)に長押しを追加: 2秒で確認画面(黄「HOLD TO REBOOT」)に切り替えつつその時点でキューを先回り退避、5秒で実際に再起動。短押しでの画面反転は変更なし。実行はリモート再起動と同じ`restartRequested`の仕組みに合流させ、Uploaderの「2xxが返るまで捨てない」不変条件は破らない。`docs/design.md`§送信の信頼性に元々あった構想（未定事項）の実装。firmware両env・`test/run.sh`成功、**実機での動作確認はまだ** | [log/2026-08-08-emergency-reboot-button.md](log/2026-08-08-emergency-reboot-button.md) |
| 2026-08-08 | **device1の新しい再起動(10:10 JST、PR #36マージ直前)を調査し、WDT panic説にたどり着いた（実装は無し）。** device1の`boot_epoch_us`更新とdevice2の受信停止がほぼ同時刻で、家庭内ネット瞬断が両機に影響したのは確か。コードを読み、`uploaderTask`のtask watchdog(10秒・panic)がTLSハンドシェイクの内部タイムアウト(120秒)より先に発火し、ハンドシェイク中に強制パニック再起動している可能性を新しい仮説として発見。正しければ2026-08-07の70分間ゼロ退避も再起動ループとして説明できる。未検証（`esp_reset_reason()`とheapテレメトリが配信済みなら次回切り分けられる） | [log/2026-08-08-wdt-panic-hypothesis.md](log/2026-08-08-wdt-panic-hypothesis.md) |
| 2026-08-08 | **heap free/maxblockのテレメトリを実装した。** firmwareが`X-Namz-Heap-Free`/`X-Namz-Heap-Maxblock`ヘッダを毎バッチ乗せる（既存の`extraRequestHeaderNames/Values`4枠を使い切り、batch-uplink側は無変更）。保存先は「最新値の上書きだけでは調査に使いづらい」との指摘を受けCloudWatchカスタムメトリクスを選択（`common/metrics.py`新設）。terraformに`cloudwatch:PutMetricData`権限を追加。pytest 99件・firmware両env・terraform validateを確認、apply/OTA配信はまだ | [log/2026-08-08-heap-telemetry.md](log/2026-08-08-heap-telemetry.md) |
| 2026-08-08 | **batch-uplinkにTLS証明書ピン留めを実装しv1.8.0へ追従した。** `Uploader`コンストラクタに`caCert`（PEM文字列）を追加し、渡すとingest/alert接続で`setInsecure()`ではなく`setCACert()`を使うようにした（[batch-uplink#8](https://github.com/nna774/batch-uplink/pull/8)、マージ済み。未指定時は従来通りinsecureへフォールバックするのでElectabuzz側は無改修）。Namazu側`main.cpp`はOTA用に埋め込み済みの`amazon_root_ca1_pem_start`をそのまま渡すよう変更——`openssl s_client`でingest先(Lambda Function URL直)の証明書チェーンを確認したところOTA用ドメインと同じ`Amazon Root CA 1`に辿り着くため、新規PEM埋め込みは不要だった。`firmware/platformio.ini`・`terraform/build_lambda.sh`のpinを両方v1.8.0へ揃え、esp32dev/adxl355両envのビルド・lambda zipビルド・pytest 149件・wireバイト等価テストを確認した。PR #36をmasterへマージし、`tools/publish_ota.sh esp32dev`→`tools/request_ota.py`で**実機device1へpull型OTAで配信し本番デプロイ済み**（`fw_version`=`378c258`着地を確認。device2への配信はまだ。terraform applyは今回不要と判断） | [log/2026-08-08-uplink-v1.8.0-ca-cert-pin.md](log/2026-08-08-uplink-v1.8.0-ca-cert-pin.md) |
| 2026-08-08 | **`detectlab.py`に複数デバイスの重ね描き解析モード（`--device 1 2`）を追加した。** 茨城県北部M3.8の事後解析（下の行）で2機の図を並べて見比べる不便さから着手。STA/LTA比・直線性はどちらも振幅二乗和・共分散固有値という回転不変量なので、`docs/device_overlay.md`でまだ未着手の方位較正が無くても方位に関わらず比較できる（同ドキュメント§3-cの発想を診断ビューに先取り適用）。生波形・スペクトログラムは軸の向きが機体ごとに違うため重ねず、x軸は`--eew`発生時刻からの経過秒で揃えてサンプルクロックが独立な2台でも実時刻ベースで並べた。茨城M3.8のログに実例（2台が発生+48秒でぴったり一致して山を作る図）を追記 | [log/2026-08-08-ibaraki-m3.8-post-hoc-detection.md](log/2026-08-08-ibaraki-m3.8-post-hoc-detection.md) |
| 2026-08-08 | **茨城県北部M3.8（震央距離161km）を正式イベントとしては逃したが、事後解析では拾えていたと確認した（実装は無し）。** `/events`に該当時刻の新規イベントは無く、`firmware`のリアルタイム閾値には未達。ただしS3生波形を`detectlab.py`で事後解析すると、device1/device2とも予測S波到達窓の直後にSTA/LTA山（device2は閾値4をわずかに超過）・直線性0.8-0.9への跳ね上がりが同時に見え、`docs/noise.md`の過去2例と同水準の**probable detection**と判断。同ファイルの「検出できた／できなかった」に事例を追記した | [log/2026-08-08-ibaraki-m3.8-post-hoc-detection.md](log/2026-08-08-ibaraki-m3.8-post-hoc-detection.md) |
| 2026-08-08 | **batch-uplinkにTLS接続使い回しを実装しv1.7.0へ追従した。** `Uploader::postBatch()`が毎回新規に`WiFiClientSecure`/`HTTPClient`を作りTLSハンドシェイクをやり直していたのを、`client_`/`http_`をメンバへ昇格し`HTTPClient::setReuse(true)`で使い回すよう修正（[batch-uplink#7](https://github.com/nna774/batch-uplink/pull/7)、マージ済み）。`firmware/platformio.ini`・`terraform/build_lambda.sh`のpinを両方v1.7.0へ揃え、esp32dev/adxl355両envのビルド・pytest 98件・wireバイト等価テストを確認した。**実機device1へpull型OTAで配信し本番デプロイ済み**（通常運用が正常なことは確認、バックフィル時の効果自体はまだ未確認） | [log/2026-08-08-uplink-v1.7.0-conn-reuse.md](log/2026-08-08-uplink-v1.7.0-conn-reuse.md) |
| 2026-08-08 | **家庭内ネット障害でdevice1が約70分欠測した件を調査した（実装は無し）。** device1はバックフィル無しで70分丸ごとデータが消失＝RAM→LittleFS退避まで到達できないほどタスクごとフリーズしていたと判明。device2も一見自動復帰したが、バックフィル最中の22:13:39〜22:18:57にもう一度誰も触らず測定が止まっており未解明。副産物としてwatchdogのSlack個人メンション追加コミットが一度もoriginにpushされず本番未反映だったデプロイ漏れを発見・修正し、ローカルmasterとoriginの32コミット乖離もマージ→push→本番4Lambda再デプロイで解消した。次に考えるべき事項（`esp_reset_reason`記録・クラッシュ前自動退避・手動ボタン退避・バックフィル起因クラッシュの原因調査）は`docs/design.md`「送信の信頼性」節と本ログに残した | [log/2026-08-08-device1-outage-and-deploy-drift.md](log/2026-08-08-device1-outage-and-deploy-drift.md) |
| 2026-08-07 | **ダッシュボードのライブ表示（1分窓）に、クライアント側だけで計算する概算震度を追加し、本番デプロイして実データで確認した。** memo.mdの要望「ガル表示だけの画面に震度も足せないか」を調査した結果、既定の1分窓ですら`/recent`のMAX_POINTS(3000)超で既にenvelope化されており生サンプルが無いと判明。一方S3読み出しはenvelope化と無関係(`load_window`は間引き前から窓の生データを全部読んでいる)なので、MAX_POINTSを6000(=1分@100Hz)に上げてもS3スキャンは増えないとユーザーに提示し、1分窓をrawで返す方針の了承を得た。震度計算は`tools/jismo/realtime.py`のFIR版(firmwareと同じ511tap係数)をapp.jsに移植し、追加のAPI呼び出し無しで動く。Python参照値とNode.js版が完全一致、pytest 98件通過。PR #29(`/recent`高速化)のmasterマージを取り込んでからterraform apply・dashboard sync/invalidationでデプロイし、本番`/recent?minutes=1`が`mode: raw`に変わったこと、実機device1の静穏データに「概算震度0(計測震度-0.5)」が表示されることをヘッドレスChromeで確認した | [log/2026-08-07-dashboard-client-side-intensity.md](log/2026-08-07-dashboard-client-side-intensity.md) |
| 2026-08-07 | **`/recent`が要求分数によらず一定時間（数秒）かかる不具合を直し、本番デプロイして改善を確認した。** `load_window`がS3キーを「時間帯(hour)+device」までしか絞れないPrefixで列挙した後、窓外のバッチも含めて**全部GETしてから**捨てていたのが原因（本番実測でdevice1=101本/device2=205本のGETがその時間帯ぶん飛んでいた）。キー名に埋め込まれた`startus`をGET前にパースして窓外を弾くようにし、GET数を実際に必要な数本まで削減。テスト追加、pytest98件確認済み。デプロイ後、device2の`/recent?minutes=1`が5.6秒→0.6〜0.8秒に短縮したことを確認した。副産物として`.claude/settings.json`を新設し、このリポジトリ限定でterraform apply/planの確認を省略できるようにした | [log/2026-08-07-recent-endpoint-unbounded-get-fix.md](log/2026-08-07-recent-endpoint-unbounded-get-fix.md) |
| 2026-08-07 | **稼働時間・再起動検知を実機2台(device1/device2)にOTA配信し、デバイス一覧にも「稼働時間」列を追加した。** `publish_ota.sh`→`request_ota.py`で両機とも`fw_version=ebcdfbf`に着地、`boot_epoch_us`/`uptime_s`が実機で埋まることを確認。一覧テーブルは列が8つになり狭い画面で収まらないため、「最終受信」の日/時を明示改行し480px以下で累計バッチ・版数を隠す設計にしたが、実機(iPhone 15 Pro、Safariのページ拡大/縮小75%表示)で確認したところ`window.innerWidth`がデバイス幅より大きく(393pt÷0.75≈524px)報告されブレークポイントを取りこぼし、ページ全体が右にはみ出す不具合を踏んだ。ブレークポイントを700pxに広げ、`.table-scroll`(overflow-x:auto)で囲む安全策も追加。検証にはheadless Chromeの500px幅下限を回避するため同一originのiframeでビューポートを独立シミュレートする手法を使った | [log/2026-08-07-uptime-rollout-and-devices-list-uptime-column.md](log/2026-08-07-uptime-rollout-and-devices-list-uptime-column.md) |
| 2026-08-07 | **稼働時間(uptime)・再起動検知を実装した。** firmwareは`X-Namz-Uptime-Us`ヘッダに`esp_timer_get_time()`の生値を毎バッチ乗せる（`kExtraRequestHeaderNames/Values`を2枠に拡張）。副産物として見つけていた`checkAndPerformPullOta()`の`millis()`折り返しバグも`esp_timer_get_time()`ベースに直して同時に修正。ingestは`boot_epoch_us = batch_start_us - uptime_us`を逆算し、既存の`devices.get_device()`呼び出しを使い回して追加のDynamoDB読み取り無しで前回値と比較、判定ロジックは`device_meta.should_update_boot_epoch()`という副作用の無い関数に切り出した(`devices.evaluate()`と同じパターン)。apiは`boot_epoch_us`/`uptime_s`を公開し、ダッシュボードのデバイス詳細ページに「稼働時間」行を追加。`test/run.sh`・`pio run`(esp32dev/adxl355両env)・pytest(97件)確認済み。**実機・本番デプロイはまだ** | [log/2026-08-07-uptime-implementation.md](log/2026-08-07-uptime-implementation.md) |
| 2026-08-07 | **稼働時間の運び方を、wireトレイラーからHTTPリクエストヘッダ(X-Namz-Uptime-Us)に作り直した（実装は未着手のまま、設計文書のみ）。** ユーザー指摘で「トレイラーだと不要なのにraw/へ90日保存され続けないか」と判明。fw_versionが既にX-Namz-Fw-Versionヘッダで送られていて、batch-uplinkのUploader::extraRequestHeaderNames/Values(v1.6.0)が「値を毎回書き換えてよい」設計だとソースで確認できたため、同じ経路に乗せることにした。batch-uplinkの変更・バージョンpin変更ともに不要になり、wire v2トレイラー案より軽い実装で済む。「センサ測定値→トレイラー／プロセス状態→HTTPヘッダ」という使い分けの原則も整理した | [log/2026-08-07-uptime-header-not-trailer.md](log/2026-08-07-uptime-header-not-trailer.md) |
| 2026-08-07 | **稼働時間(uptime)・再起動検知の作戦をまとめた（実装は未着手）。** ファームは`esp_timer_get_time()`(起動からのus、事実上折り返さない)の生値をwire v2の新トレイラーで送るだけにし、サーバ(ingest)が`boot_epoch_us = batch_start_us - uptime_us`を毎バッチ逆算・前回値と比較して再起動を検知する設計にした(`fw_version`変化を見るより確実、クラッシュ等も拾える)。保存はセンサ種別と同じ`namazu-devices`への部分update_itemパターン。`millis()`ではなく`esp_timer_get_time()`を選んだ理由の調査中に、`checkAndPerformPullOta()`のpull型OTA再試行バックオフに`millis()`49.7日折り返し由来の潜在バグ(直接比較のため折り返し境界の最大60秒間だけバックオフを早期満了と誤判定しうる。実害は小さい)を発見、稼働時間実装と同時に直す方針とした | [log/2026-08-07-uptime-strategy.md](log/2026-08-07-uptime-strategy.md) |
| 2026-08-07 | **デバイス詳細ページにセンサ種別・版数リンク・イベント導線を追加した。** センサ種別はバッチヘッダに毎回乗っているので、ingestが受信ついでに`namazu-devices`へ別途`update_item`で記録（batch-uplinkの`devices.record_batch()`がupdate_itemの部分更新と確認できたので安全と判断。新設`lambda/common/device_meta.py`は`ota_watch.py`と同じ「Namazu固有の属性はbatch-uplinkに置かずlambda/common側で持つ」方針）。版数リンクはfw_versionがgit短縮hashそのものなのでGitHubコミットへ直結、イベント導線は既存の`?device=<id>`絞り込みへのハッシュを組むだけ。uptimeはファームが起動時刻を送っていないため計算不可と判断し見送った。pytest 90件通過、モックAPIでブラウザ確認済み | [log/2026-08-07-device-detail-sensor-and-links.md](log/2026-08-07-device-detail-sensor-and-links.md) |
| 2026-08-07 | **温度トレンドの取得を、S3 Range GET(下の行)からingest時DynamoDB記録に作り直した。** ユーザー指摘で「S3のGET料金はリクエスト数課金でサイズに依らないので、ヘッダ→トレイラーの2分割はむしろリクエスト数が倍になる」と判明。ingestは受信バッチを既に`wire.parse()`しているので温度トレイラーの取り出しに追加のS3アクセスは不要と気づき、新設DynamoDBテーブル`namazu-device-temp`(TTL付き)にingestが1件PutItem、api `/devices/<id>/temp`はQueryするだけにした。読み取り(公開API)のコストが「見られる回数」から解放され、書き込み頻度(バッチ受信頻度)だけに固定された。`store.load_temp_series`・`wire.parse_header`/`payload_size`は削除。api応答形式・ダッシュボード側は無改修。pytest 86件通過、terraform validate(-backend=false)確認済み | [log/2026-08-07-device-temp-move-to-dynamodb.md](log/2026-08-07-device-temp-move-to-dynamodb.md) |
| 2026-08-07 | **ダッシュボードにデバイス個別ページ`#device/<id>`を追加し、センサ内蔵温度のトレンドチャートを置いた。** 一覧テーブルには列を足さず（ユーザー指摘で「詰め込みすぎ」判断）、`#event/<id>`と同じ一覧⇔詳細の排他表示パターンを流用。温度は分〜時間スケールのドリフト用途で波形とはサンプル間隔が3桁違うため、ライブ波形とは別画面にした。新設`GET /devices/<id>/temp`はヘッダ+トレイラーだけをRange GETし（`wire.parse_header`/`store.load_temp_series`）、1バッチ数十KBのペイロード転送を避けて24時間窓まで許容する。ライブビューへの温度重ね描き案は実装せず、アイデアとしてログに残した。pytest 86件通過、モックAPIでブラウザ動作確認済み。**実機・本番デプロイはまだ** | [log/2026-08-07-device-detail-page-temp-trend.md](log/2026-08-07-device-detail-page-temp-trend.md) |
| 2026-08-06 | **OTA転送中はTFTの震度画面を専用の「更新中」画面（紫背景 + "OTA UPDATING"）に差し替えた。** 測定タイマーが止まる間、震度・WiFi・バックログが凍った古い値のまま表示され続けると「地震計が壊れた」と誤読されるため。日時は引き続き更新するので凍結検知の役目は保てる | [log/2026-08-06-ota-display-updating-screen.md](log/2026-08-06-ota-display-updating-screen.md) |
| 2026-08-06 | **pull型OTAの「停滞」誤検知を直した。** device1が`a93433a`へ実際には成功していたのに、`pending_ota_version`が達成後もサーバ側でクリアされず残り続けるせいでwatchdogが30分後に「停滞」を誤通知していた。ingestが受信バッチの`fw_version`と`pending_ota_version`の一致を検知したら自動でクリアするようにした（`ota_watch.reached_target()`/`clear_ota_target()`）。USB焼き直し時の自動差し戻り事故（device1-16mb-confirm.md）も同じ根だったので合わせて解消 | [log/2026-08-06-ota-stuck-false-positive.md](log/2026-08-06-ota-stuck-false-positive.md) |
| 2026-08-06 | **device1をesptool flash_idで物理確認し、device2と同じ16MBフラッシュと判明。パーティション表・flash_size設定を`[env:esp32dev]`base側に集約し、device1/device2共通の`partitions_16mb.csv`（リネーム）に揃え、実機device1にも反映して本番復帰させた。** 途中、USB書き込み直後に古いバージョンの`pending_ota_version`が残っていたせいでpull型OTAが自動で差し戻す事故を踏み、正規のOTA経路（publish_ota.sh→request_ota.py）で収束させて回復した | [log/2026-08-06-device1-16mb-confirm.md](log/2026-08-06-device1-16mb-confirm.md) |
| 2026-08-06 | **fw_version報告つきファーム(c64f379)をdevice1にもOTA配信した。** `ota/esp32dev/c64f379.bin`を新たにビルド・公開し、`tools/request_ota.py request 1 c64f379`で許可。次のバッチ送信サイクルで`/devices/1`の`fw_version`がc64f379に着地し、device2と同じ版数で揃った | [log/2026-08-06-ota-device1-fwversion-rollout.md](log/2026-08-06-ota-device1-fwversion-rollout.md) |
| 2026-08-06 | **CloudFrontのエッジキャッシュとブラウザキャッシュを別レイヤーで分離した。** 前PRでS3オブジェクトへ`Cache-Control: no-cache`を付けたところ、CloudFrontがCache PolicyのDefaultTTLよりオリジンの鮮度ヘッダーを優先するため、デプロイのたびにinvalidationしているのにエッジ↔S3間の再検証が毎回発生していた（ユーザー指摘で発覚）。`cache_policy_id`をManaged-CachingOptimizedに変えただけでは不十分と実測（`x-cache: RefreshHit`が続く）で判明し、`aws_cloudfront_response_headers_policy`でビューワー向けにだけ`no-cache`を注入する方式に直した。S3側のCache-Controlは撤去。適用後`x-cache: Hit from cloudfront`（エッジ完結）を確認しつつブラウザ向けヘッダーは維持できている | [log/2026-08-06-dashboard-cloudfront-cache-layering.md](log/2026-08-06-dashboard-cloudfront-cache-layering.md) |
| 2026-08-06 | **デバイス生存台帳に版数(fw_version)を記録し、ダッシュボードに再起動要求・OTA適用状況を出した。** batch-uplinkの`Uploader`に固定リクエストヘッダ送信API(複数対応、`extraRequestHeaderNames/Values`)を追加してv1.6.0を切り（[PR#6](https://github.com/nna774/batch-uplink/pull/6)）、firmwareが毎バッチ`X-Namz-Fw-Version`を送るようにした。api `/devices`が`fw_version`・`pending_restart_requested_at_us`を公開し、ダッシュボードのデバイス一覧に版数・OTA列を追加、再起動要求はイベント一覧のbadge-art/badge-manualと同じ考え方で状態セルへのインラインバッジにした。`docs/ota.md`§7未決事項1（サーバがデバイスの現在版数を知らない問題）を解消。firmwareビルド全env・`test/run.sh`・pytest(119件)・ダッシュボードのブラウザ動作に加え、**device 2実機でOTA配信〜着地まで確認済み**（`fw_version`がc64f379で着地、欠測なし）。デプロイ時にブラウザがapp.jsだけ古いキャッシュを抱え込む事故も踏み、`--cache-control 'no-cache'`をデプロイ手順に追加した | [log/2026-08-06-device-status-fw-version-header.md](log/2026-08-06-device-status-fw-version-header.md) |
| 2026-08-06 | **device1もNVS化・pull型OTA対応にし、実機で自己更新（esp32dev envでは初）まで成功した。** PR#13マージ・watchdog再デプロイ後、device2で確立した手順（provision-h→[env:provision]→本体ファーム→実際にOTAで一度更新させて検証）をdevice1にもそのまま適用。両機とも本番稼働に復帰し、手元から`tools/request_ota.py`一発で外出先からの更新ができる状態になった | [log/2026-08-06-ota-device1-prep.md](log/2026-08-06-ota-device1-prep.md) |
| 2026-08-06 | **pull型OTAをdevice2実機で通しで試し、TLS証明書検証（2段階失敗の末Amazon Root CA 1埋め込みで解決）・失敗時の高頻度リトライ（測定停止を招いた。1分バックオフで解消）・停滞の未検知（watchdogにSlack通知を追加）の3件を直した。** 実際に取得〜書き込み〜再起動〜新バージョンでの起動まで成功し、pull型OTAが実機で動いた最初の記録になった | [log/2026-08-06-ota-pull-device2-live-test.md](log/2026-08-06-ota-pull-device2-live-test.md) |
| 2026-08-06 | **pull型OTAのトリガーを作戦どおり（バッチ送信レスポンスへの便乗）に戻した。** 直前の実装で「api Lambdaへの独立GET」に無断で設計変更していたことをユーザーに指摘され、batch-uplinkを拡張（`Uploader::watchResponseHeaders`で複数ヘッダ監視に対応、[PR#5](https://github.com/nna774/batch-uplink/pull/5)・[v1.5.0](https://github.com/nna774/batch-uplink/releases/tag/v1.5.0)）して当初案どおりに作り直した。`firmware/platformio.ini`・`terraform/build_lambda.sh`のpinをv1.5.0へ揃えた。教訓: 設計を実装都合で無断変更しない、必ず確認する | [log/2026-08-06-ota-pull-trigger-revert-to-header.md](log/2026-08-06-ota-pull-trigger-revert-to-header.md) |
| 2026-08-06 | **HTTPSプル型OTAを実装した。** 前提のNVS化（デバイス識別情報・秘密・エンドポイントURLをコンパイル時定数からPreferencesへ移行、`[env:provision]`/`[env:adxl355-provision]`で書き込み）・ビルド時バージョン埋め込み（gitの短縮hash、`get_fw_version.py`）・`tools/request_ota.py`（手元から更新許可）・api Lambdaでの`pending_ota_version`公開・ファームのOTAチェック(5分ごとのapi問い合わせ)+`esp_https_ota`取得（TLSはArduino-ESP32同梱のルートCAバンドルで検証）・`tools/publish_ota.sh`（既存ダッシュボードCloudFront/S3への配布）を実装。トリガーは当初案（バッチ送信レスポンスへの便乗）から、batch-uplinkを変更せずに済むapi Lambdaへの独立GETに変更した。ロールバックは実機無しでは安全に検証できず見送り。firmwareビルド全env・`test/run.sh`・pytest(113件)は確認済み。**実機での動作確認はまだ** | [log/2026-08-06-ota-pull-implementation.md](log/2026-08-06-ota-pull-implementation.md) |
| 2026-08-06 | **pull型OTA作戦を訂正: バイナリに秘密情報が生で入っていると判明した。** `firmware/src/secrets.h`はWiFi SSID/パスワード・投稿用HMAC鍵・OTAパスワードをコンパイル時の文字列定数として持ち、焼いたbinに平文で入る。push型は送り返す相手が秘密の持ち主自身なので問題にならないが、pull型で「envごとに1本を公開URLに置く」設計は特定デバイスの秘密を世界に漏らすことになり成立しない。対策として秘密情報をNVS化（初回USB書き込み時に別途書く、OTAはappパーティションのみ書き換えるので保持される）する方針を、pull型実装の前提条件として追加した | [log/2026-08-06-ota-pull-secrets-in-binary.md](log/2026-08-06-ota-pull-secrets-in-binary.md) |
| 2026-08-06 | **HTTPSプル型OTA（外出先からの更新・無人運用向け）の作戦をまとめた（実装は未着手）。** トリガーは自律ポーリングではなく手元からの明示許可方式を採用（配布物の書き込み権限侵害時のブラスト半径を抑えるため）。伝達経路はリモート再起動と同型だが値は消費せず照合し続ける設計にし、それ自体がリトライ機構になる。配布物は既存CloudFront+S3に相乗り。ロールバックはpull型で初めて`CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE`の価値が出ると判断した | [log/2026-08-06-ota-pull-strategy-design.md](log/2026-08-06-ota-pull-strategy-design.md) |
| 2026-08-06 | **device 2にOTA対応ファームをUSB書き込みし、push OTAが自宅ネットワークのVLAN分離で母艦から届かないと判明した。** USB書き込み・起動・ArduinoOTAリスナー起動（`namazu-2.local`）・クラウド受信再開は実機で確認できたが、母艦(`10.8.30.0/24`)からdevice 2(`10.255.255.0/24`, `unnamed_network_g`)へのespota転送はICMPは通るのにUDP往復が無応答。ゲスト系VLANのクライアント分離が疑わしい。ファーム実装の不具合ではなく運用上の制約として`docs/ota.md`に記録した | [log/2026-08-06-ota-network-isolation.md](log/2026-08-06-ota-network-isolation.md) |
| 2026-08-06 | **OTA更新（ArduinoOTA、LAN内push）を実装した。** batch-uplinkに`Uploader::flushToSpill()`を追加してv1.4.0を切り（[PR#4](https://github.com/nna774/batch-uplink/pull/4)）、firmwareに焼く前のRAMキュー退避＋測定停止シーケンスを実装。`tools/provision_device.py`にデバイス別`ota_password`を追加し、`platformio.ini`に`esp32dev-ota`/`adxl355-ota`envを足した。同じ`flushToSpill()`をリモート再起動にも配線し直し、通信完了を待たず数秒で再起動できるようにした。firmwareビルド(esp32dev/adxl355/両-ota/両-sensortest)・`test/run.sh`・`pytest`(113件)は確認済み。**実機での動作確認はまだ** | [log/2026-08-06-ota-arduino-ota.md](log/2026-08-06-ota-arduino-ota.md) |
| 2026-08-06 | **リモート再起動を実装した。** batch-uplinkに`devices.request_restart`/`Uploader.watchResponseHeader`を追加してv1.3.0を切り、ingest/tools(`request_restart.py`)/firmware(`main.cpp`)を実装した。firmwareビルド(esp32dev/adxl355両env)・`pytest`(111件)は確認済み。**実機での動作確認はまだ** | [log/2026-08-06-remote-restart-implementation.md](log/2026-08-06-remote-restart-implementation.md) |
| 2026-08-06 | **リモート再起動（コマンドラインから要求→デバイスがバッチ送信時に検知して自分で再起動）の設計をまとめた（実装は未着手）。** 伝達経路はバッチ送信レスポンスへの便乗を採用。batch-uplinkの`Uploader::postBatch`にレスポンスヘッダ読み取りを追加する必要があるが、`dropOldestWhenFull`と同じくオプトイン方式にしてElectabuzz側の挙動は変えない設計にした | [log/2026-08-06-remote-restart-design.md](log/2026-08-06-remote-restart-design.md) |
| 2026-08-05 | **TFT画面右下のbacklog表示を「件数+経過時間」(`buf:12 18m`)に拡張した。** 件数は`spillCount()`だけでなく`spillCount()+ramQueued()`の合計に修正（従来RAM分が抜けていた）。経過時間はbatch-uplinkに追加した`Uploader::oldestQueuedStartUs()`([batch-uplink#2](https://github.com/nna774/batch-uplink/pull/2)、マージ済み・`v1.2.0`)を使う。`lib_deps`/`UPLINK_VERSION`をv1.2.0に上げてビルド確認済み。**実機での目視確認はまだ** | [log/2026-08-05-display-backlog-age.md](log/2026-08-05-display-backlog-age.md) |
| 2026-08-05 | **batch-uplinkに`v1.1.0`タグを切ってNamazu側(`lib_deps`/`UPLINK_VERSION`)を追従させ、`main.cpp`に`dropOldestWhenFull=true`を配線した。** その過程で、直前にマージしたPR#4に本来含まれるはずのflash_size修正コミットが1本漏れていた（マージ後にpushしたため）と判明し、cherry-pickで直接masterに修正を入れた。マージ操作は「ボタンを押した時点のHEAD」しか取り込まないという教訓 | [log/2026-08-05-uplink-v1.1.0-and-merge-gap.md](log/2026-08-05-uplink-v1.1.0-and-merge-gap.md) |
| 2026-08-05 | **device2のspill(LittleFS退避)実容量が既定4MB前提のコメント(`config.h`の「90日ぶんの上限目安」)と合っていないと判明。esptool flash_idで実機を物理確認したところ実は16MBあり、`firmware/partitions_adxl355_16mb.csv`（OTA用app0/app1各2MB・spiffs約11.88MB）に書き換えて実機で正常動作（起動・WiFi接続・送信再開）まで確認した。** spill実容量は約19.5分→**約168.8分（約2時間49分）**。**PlatformIOのflash_size上書きキーは`board_build.flash_size`ではなく`board_upload.flash_size`**（前者は黙って無視される。これで一度起動ループを踏んでからの復旧＋原因特定を経ている）。それでも溢れる場合に備え、**batch-uplinkに「spill満杯なら古いデータから捨てる」動作をオプトインで追加する方針**（Electabuzzの既定動作は変えない） | [log/2026-08-05-device2-spill-overflow.md](log/2026-08-05-device2-spill-overflow.md) |
