# 2026-08-30 千葉県東方沖M4.9 事後解析

## きっかけ

ユーザーが気象庁電文形式の連報（第1〜12報、00:17:05発生・00:17:15〜00:18:00頃発表）を貼り、
「これどう」「まだbatch来てないかもしれない」と確認依頼。

## 震源要素（気象庁電文・第12報＝最終報）

- 発生: 2026-08-30 00:17:05
- 震源: 千葉県東方沖 N35.8/E140.9、深さ40km
- M4.9（第1報M4.4→段階的に上方修正、最終M4.9）
- 最大震度4（第1報震度3→第8報以降震度4に上方修正）

## 自動検知の有無

`/events?all=1&size=20`に該当あり。**自動確定（cloud_confirmed）済み**:

| | device1 (`0001-59600556`) | device2 (`0002-59600556`) |
|---|---|---|
| onset | 00:18:11.607 JST | 00:18:11.521 JST |
| confirmed_intensity | 1（震度1） | 1（震度1） |
| peak_gal | 1.139 | 1.151 |
| device_prompt | true（速報あり） | false |

両機ともすでに確定通知済み（`notified_confirm_ord=1`）。

## detectlab解析

`--eew "35.8,140.9,40,2026-08-30 00:17:05"` → 震央距離225km・震源距離229km。

### 標準設定（3軸1-10Hz、10分）

| 項目 | device1 | device2 |
|---|---|---|
| STA/LTA peak | 5.32 | 5.59 |
| P窓 SNR/直線性 | 1.12 / 0.52 → 微妙 | 1.21 / 0.47 → 微妙 |
| S窓 SNR/直線性 | 3.96 / 0.68 → 地震らしい | 5.47 / 0.70 → 地震らしい |
| コーダ想定域 SNR/直線性 | 2.44 / 0.76 → 地震らしい | 3.05 / 0.80 → 地震らしい |

両機ともSTA/LTAが閾値4を超過。`--corr-bin 20`の直線性一致度は、背景水準（t=-150〜-30s、
frac(corr≥0.6)=0.18）に対し、t=+40〜100s区間で0.65〜0.88と明瞭に高い。S波到達直後から
コーダにかけて2機が同時に一致度を上げている。

画像: [2026-08-30-chiba-oki-m4.9-10min.png](img/2026-08-30-chiba-oki-m4.9-10min.png)

**判定: probable detection**（両機で明瞭に一致）。

### 保存範囲末尾（onset+600秒=00:28:11）の先にコーダが残っていないか確認

現在時刻が保存範囲末尾に届いていなかったため、データ到達を待ってから再実行（`--at
"2026-08-30 00:26:00" --minutes 5`＝00:24:00〜00:34:00付近をraw直読み）:

- STA/LTA peak: device1=1.92、device2=2.39（いずれも閾値4未達、onset候補なし）
- `--corr-bin`のfrac(corr≥0.6)はt=+340〜780s区間（00:31:40〜00:38:00付近）で0.01〜0.23
  と、標準設定解析で確認済みの背景水準(0.18)と同じレンジに収まっている

画像: [2026-08-30-chiba-oki-m4.9-tail-check.png](img/2026-08-30-chiba-oki-m4.9-tail-check.png)

標準設定解析でもt=+100s以降でfracが既に背景水準まで下がっていたことと合わせ、**コーダは
onsetから100秒程度で収束しており、保存範囲(600秒)の延長は不要**と判断した。

（この解析中、`--lead-min`既定値のままだと背景RMS推定窓に届かず警告が出るケースを踏んだ。
一般的な注意点として`docs/post_hoc_detection.md`に追記した。後述）

## detection_range.mdとの整合

`docs/detection_range.md`の目安ではM4.9→予測震源距離290km、投げる価値ありレンジ232〜464km。
実測229kmはレンジ下限のすぐ外側（予測比0.79）だが、実際には明瞭に検出できている。同表の
「M4.0で震源距離220kmが微妙・273kmがprobableという逆転が起きている」という注記通り、
目安はあくまで目安であることの追加事例。`detection_events.csv`への追記は今回は見送った
（ユーザー判断があれば追加・`tools/detection_range.py`で再生成）。

## 新しい知見: 保存済みevent波形だけでは背景RMS推定に届かないことがある

ユーザーからの指摘（「rawは消えるからeventにした時に前の部分の波形も保存しておくのが正しいのでは」）
を受けて整理した。

- `detectlab.py --eew`の背景RMS推定窓は「**発生時刻**の-150秒〜-30秒」の2分間
- 自動確定イベントの保存前マージン`PRE_SECONDS=30`（`lambda/detect/handler.py`）は
  **onset**（検知時刻）基準で、背景比較用ではなく検知直前の安全マージン目的
- onsetは常に「発生時刻+伝播時間」より後に来るため、伝播時間が30秒を超える地震では
  保存済みevent波形の先頭がすでに発生時刻より後ろになり、背景RMS推定窓に**まったく
  届かない**。今回のケースでは伝播時間約66秒に対しPRE_SECONDS=30だったため、保存開始
  （00:17:24）は発生（00:17:05）の19秒後——本当に地震の影響を受けていない区間は
  P波到達（00:17:34）までの10秒しかなかった
- rawは90日で消えるため、この背景ぶんを後から取り直せるのは90日以内に限られる。恒久
  保存であるべきevent側にこそ、この背景マージンが足りていないことになる

対応案として`PRE_SECONDS`を手動昇格の既定`--pre 180`程度まで引き上げることを提案したが、
detect Lambdaのデプロイ（`build_lambda.sh`+`terraform apply`）と保存量増加を伴うため、
実装はユーザー確認待ちで保留。`docs/post_hoc_detection.md`には、この罠自体（`--event`で
背景比較をする時は`--from-raw`が要ること）を新しい節として追記した。

## 次に可能になったこと

特になし（既存の自動確定を裏取りしただけで、コード変更は無し）。`PRE_SECONDS`引き上げの
実装可否は次回以降の判断待ち。

## 新しい知見の反映先

- `docs/noise.md`「検出できた／できなかった」に今回の事例を追記
- `docs/post_hoc_detection.md`に「保存済みevent波形だけでは背景RMS推定に届かないことがある」節を追記
