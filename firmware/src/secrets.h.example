#pragma once
// このファイルを secrets.h にコピーして値を埋める。secrets.h は gitignore 対象。

#include <cstdint>

static constexpr const char* kWifiSsid = "your-ssid";
static constexpr const char* kWifiPass = "your-pass";

// ingest Lambda の Function URL（30秒バッチのPOST先）
static constexpr const char* kIngestUrl = "https://xxxxx.lambda-url.ap-northeast-1.on.aws/";
// alert Lambda の Function URL（デバイス速報のPOST先）。ingestと同じでもよい。
static constexpr const char* kAlertUrl = "https://xxxxx.lambda-url.ap-northeast-1.on.aws/alert";

static constexpr uint32_t kDeviceId = 1;

// HMAC-SHA256 共有鍵（ingest側の device_id->secret と一致させる）
static constexpr const char* kHmacSecret = "change-me-32bytes-random-secret";
